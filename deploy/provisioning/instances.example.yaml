# Registre des instances — EXEMPLE FICTIF (OPS02, F12 §6.3).
#
# Le registre RÉEL (deploy/provisioning/instances.yaml) est tenu à jour par new-instance.ps1 /
# update-instance.ps1 et n'est JAMAIS versionné (.gitignore) : il porte des données opérateur
# (noms d'éditeurs, URLs d'instances). Ce fichier-ci ne sert que de RÉFÉRENCE DE FORMAT — toutes
# les valeurs y sont fictives (CLAUDE.md n°7 : aucune donnée client dans le code).
#
# Aucun secret ici (mots de passe / clés → le .env de chaque instance, gitignoré). Les instances
# self-hosted n'apparaissent que si l'éditeur a souscrit la méta-supervision (OPS04).
instances:
  - name: "demo-editeur"
    editor: "Éditeur de Démonstration SARL"
    url: "https://liakont.demo.example"
    hosting: "hosted"
    version: "2026.06.11-1200"
    project: "liakont-demo-editeur"
    created_at: "2026-06-01T09:00:00Z"
    updated_at: "2026-06-11T12:00:00Z"
  - name: "exemple-selfhosted"
    editor: "Exemple Self-Hosted SAS"
    url: "https://liakont.exemple.test"
    hosting: "self-hosted"
    version: "initial"
    project: "liakont-exemple-selfhosted"
    created_at: "2026-06-05T14:30:00Z"
    updated_at: "2026-06-05T14:30:00Z"

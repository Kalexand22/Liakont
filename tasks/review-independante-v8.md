# Review indépendante — Liakont backlog v6
Date : 2026-06-03
Reviewer : agent indépendant (session be8af6a1-2537-4288-b9f7-ba09260f34f3)

Périmètre : dépôt Liakont (main, HEAD 4613521), dépôt d'état `C:\Source\liakont-orchestration`
(lecture seule), dépôt Stratum `C:\Source\Stratum` (lecture seule), historique v5 via
`git show 223f0f3~1`. Dix dimensions (A-J) examinées par subagents parallèles ; chaque finding
ci-dessous a été vérifié sur pièce par le reviewer principal avant inclusion. Un finding P1
remonté par un subagent (faux vert `$script:Base` dans codex-review.ps1) a été **infirmé par test**
et écarté.

## Synthèse
- Findings P1 : **11**
- Findings P2 : **39**
- Verdict global : **corrections requises avant lancement de l'orchestration.**

Le backlog v6 est structurellement sain (zéro item orphelin, zéro cycle, manifest ↔ state ↔ lots
parfaitement alignés, renumérotation TRK intègre, aucune perte de contenu fiscal v5). Les 11 P1
se répartissent en trois familles : (1) **hypothèses fausses sur le socle Stratum** — trois briques
supposées « directes » ne le sont pas (Identity→Party, email stub, jobs sans tenant) et feront
échouer SOL01/SUP03/PIP/TRK06 ; (2) **trous fonctionnels et fiscaux** — cohérence AA/AAA, routage
Flux 10.2/10.4, échéances déclaratives, gestion des agents, auto-update côté plateforme ;
(3) **incohérences d'outillage** — infra E2E inexistante + contradiction sur son exécution, et une
contradiction prompt/protocole qui peut ressusciter des items terminés. Tous sont corrigeables
dans le backlog et la doctrine sans remettre en cause le pivot (estimation : 1-2 sessions
interactives de correction).

## Findings P1

### Hypothèses sur le socle Stratum (dimension J)

[P1] | orchestration/items/SOL.yaml:21,25-27 | SOL01 exige de vendorer le module Identity SANS le module Party (« Ne PAS copier les modules métier de l'ERP (Party, Sales, …) »), or `C:\Source\Stratum\src\Modules\Identity\Application\Stratum.Modules.Identity.Application.csproj:10` contient un `ProjectReference` dur vers `Party.Contracts`, réellement utilisé (`CreateUserHandler` injecte `IPartyQueries`). Le périmètre de copie décrit est incompilable : `dotnet build src/Liakont.sln` échouera et SOL01 — l'item fondateur de tout le backlog — bloquera. | Trancher dans SOL01 : soit vendorer AUSSI `Party.Contracts` (il ne dépend que de Common.Abstractions — copie minime), soit patcher Identity pour retirer la dépendance (modification de `Stratum.*` à consigner dans la provenance). Documenter le choix dans la description de SOL01.

[P1] | orchestration/items/SUP.yaml:61-85 (SUP03) + tasks/analyse-impact-pivot-plateforme.md:147 | SUP03 (« envoi des alertes par email via le module Notification du socle ») et l'analyse d'impact (« Notifications email → Module Notification → ✅ Direct ») supposent que le socle envoie des emails. Faux : le seul transport enregistré dans Stratum est un stub — `NotificationModuleRegistration.cs:27` enregistre `StubEmailTransport` qui ne fait que logger, et aucun package SMTP (MailKit/MimeKit/System.Net.Mail) n'existe dans `Directory.Packages.props`. Le pipeline (templates, queue, retry) existe, le transport n'existe pas. | Ajouter à SUP03 l'implémentation d'un `IEmailTransport` SMTP réel + l'ADR de dépendance (MailKit ou équivalent, règle blueprint §5), et corriger la ligne 147 de l'analyse d'impact (statut 🟡, pas ✅).

[P1] | orchestration/items/PIP.yaml:24,42-43 + SUP.yaml:11-13 + TRK.yaml:206 + PIP.yaml:91 | Quatre items supposent que le module Job du socle exécute des jobs « par tenant » (PIP01 : « job planifié par tenant », PIP03, SUP01 : « job qui évalue tous les tenants », TRK06 : « job d'ancrage quotidien par tenant »). Faux : le module Job Stratum crée un scope nu sans résolution de tenant (`JobWorker.cs:114`, `JobScheduler.cs:72` — aucune référence à `ITenantContext` dans tout le module) et ses tables vivent dans la base SYSTÈME. Avec database-per-tenant, un job ne peut ni s'exécuter dans la base d'un tenant ni itérer sur les tenants sans mécanique supplémentaire — qui n'est spécifiée nulle part. Risque : chaque item invente sa propre boucle multi-tenant, ou les quatre items bloquent. | Spécifier UNE fois la mécanique (handler de job qui liste les tenants et bascule la connexion par tenant) — soit comme critère explicite de PIP01 (le premier consommateur, les autres items le référencent), soit comme item socle dédié dont PIP01/PIP03/SUP01/TRK06 dépendent.

### Trous fonctionnels et fiscaux (dimensions C, E, F)

[P1] | orchestration/items/VAL.yaml:88-92,101 (VAL04) | `CategoryRateConsistencyRule` (Blocking) ne couvre que S→taux>0 et E/Z/AE/G/K/O→taux=0, alors que le référentiel UNCL5305 du produit comprend 9 catégories dont AA (taux réduit 10 %/5,5 %) et AAA (super réduit 2,1 %) — PIV.yaml:19, TVA.yaml (TVA01/TVA04). Une ligne mappée AA ou AAA avec un taux 0 (erreur de table ou donnée source fausse) passe la validation et part à la PA : c'est exactement le « envoyer faux » que la décision du 2026-06-02 (amendement F04 §3.4) voulait bloquer. Trouvé indépendamment par deux dimensions de review (C et F). | Étendre VAL04 (et amender F04 §3.4 + la décision) : AA→taux>0, AAA→taux>0, en traçant la source (F03 §2.1 : 10/5,5→AA, 2,1→AAA). Si l'omission est volontaire, la documenter explicitement dans VAL04.

[P1] | orchestration/items/PIP.yaml:107 (PIP03) + PAA.yaml:20-23 (PAA01) + PIV.yaml:25-27 (PIV01) | PAA01 et PIP03 distinguent les flux de paiement Domestic (10.4) et International (10.2) — capacités séparées, type de flux porté par `PaymentReportPeriod` — mais AUCUNE règle ne dit comment le pipeline détermine qu'un agrégat de paiement est domestique ou international. `PivotPaymentDto` (PIV01) ne porte ni pays ni indicateur de flux ; PIP03 ne spécifie pas de règle de dérivation (ex. via le pays acheteur du document lié) ; F09 ne tranche pas (la question « comment transmettre les Flux 10.2/10.4 » y est listée comme ouverte). L'agent qui implémentera PIP03 devra soit inventer une règle fiscale (interdit — règle CLAUDE.md n°2), soit bloquer l'item. | Trancher AVANT le lancement : soit acter que PIP03 ne produit que du 10.4 en V1 (B2C domestique — cohérent avec « B2B international = phase 2 ») et que 10.2 est une capacité déclarable non alimentée, soit spécifier la règle de dérivation (pays acheteur du document lié via RelatedDocumentNumber) avec sa source. L'écrire dans PIP03 et F09.

[P1] | orchestration/items/CFG.yaml:19-22,51-52 (CFG01/CFG02) vs SUP.yaml:21,44 (SUP01) et WEB.yaml:21,31 (WEB01) | SUP01 définit l'alerte « Échéance de période déclarative à J-3 avec documents non transmis → 🔴 Critique » et WEB01 affiche la « prochaine échéance de période déclarative ». Or le paramétrage fiscal du tenant (`FiscalSettings`) ne porte QUE `vatOnDebits` et `operationCategory` : aucun champ de fréquence/régime déclaratif (décadaire/mensuelle/trimestrielle — F09 §2), aucune règle de calcul d'échéance nulle part dans le backlog. L'alerte d'échéance — le garde-fou réglementaire qui justifie la supervision proactive — est incalculable, ou sera implémentée avec une cadence devinée (règle fiscale inventée). | Ajouter à CFG01/CFG02 un champ `reportingFrequency` (nullable = décision en attente, tracé vers F09 §2) consommé par SUP01/WEB01 ; tant qu'il est null, pas de calcul d'échéance (suspension + message, même mécanique que `vatOnDebits`). Alternative : retirer l'alerte d'échéance de SUP01/WEB01 en V1 (à trancher).

[P1] | orchestration/items/API.yaml (lot entier) + WEB.yaml:103 + OPS.yaml:74 vs docs/conception/F10-Console-Admin-WPF.md (amendement 2026-06-03) | La spec F10 amendée (qui fait foi) exige l'écran « **Gestion des agents** (enregistrement, clés API — voir F12 §4.2) ». Les handlers existent (PIV05 : RegisterAgent/RevokeAgent/RotateKey), mais aucun endpoint API et aucun écran WEB ne les expose après la création du tenant : OPS03 n'enregistre que le PREMIER agent (assistant de création), WEB04 n'affiche l'agent qu'en lecture seule. Impossible d'enregistrer un 2e agent, de révoquer une clé compromise ou de la faire tourner — opérations de sécurité de routine pour un parc d'agents. | Ajouter au lot API les endpoints de gestion d'agents (liste/enregistrement/révocation/rotation, branchés sur les handlers PIV05) et au lot WEB (ou OPS) l'écran « Agents » correspondant, avec clé affichée une seule fois.

[P1] | orchestration/items/AGT.yaml:116,139-140 (AGT04) + PIV.yaml:155 (PIV05) + OPS.yaml:122-149 (OPS05) | L'auto-update de la flotte d'agents n'a pas de côté plateforme. AGT04 (client) télécharge depuis `updateUrl` et PIV05 renvoie `latestAgentVersion`/`updateRequired`/`updateUrl + hash` dans le heartbeat — mais aucun item ne (a) héberge/publie le package d'agent et son hash côté plateforme, ni (b) ne permet à l'opérateur de définir la version « latest » et de déclencher le déploiement de flotte. OPS05 produit le package mais ne le publie pas. Les données que PIV05 doit renvoyer n'existent nulle part : la flotte ne pourra jamais se mettre à jour (engagement F12 §4 et blueprint §3.1 « Auto-update (flotte) »). | Créer un item (lot OPS ou PIV) « publication de version d'agent + politique de mise à jour de flotte » : registre des versions publiées, endpoint de téléchargement du package + hash, réglage de `latestAgentVersion`/`updateRequired` par instance/tenant, alimentant la réponse heartbeat de PIV05.

### Outillage et orchestration (dimensions G, H)

[P1] | orchestration/blueprints/blazor-page-item.yaml:32,51 + orchestration/items/SOL.yaml (lot SOL) + manifest.yaml:184 | Le blueprint `blazor-page-item` exige des tests E2E Playwright « running against the dev instance » qui « MUST exist and pass before the commit » — pour 10 items (WEB01-08, SUP02, OPS03). Or AUCUN item ne crée l'infra de test E2E : le harness Stratum (`tests/Stratum.Tests.E2E`, `KeycloakE2EWebFactory` qui démarre PostgreSQL + Keycloak via Testcontainers + realm fixture) vit dans `tests/`, hors du périmètre de vendoring de SOL01 (qui ne copie que `src/`) ; SOL03 ne fait que documenter la pyramide de tests. Le premier item WEB échouera au nœud `integration_tests` faute de projet E2E, de fixture realm et de navigateurs Playwright. | Ajouter un item (segment socle, ou en tête de console-web avant WEB01) qui crée `tests/Liakont.Tests.E2E` adapté du harness Stratum (Testcontainers PostgreSQL + Keycloak, realm Liakont, login POM, install navigateurs), et le déclarer dans les `depends_on` de WEB01.

[P1] | orchestration/items/SOL.yaml:100-101 (SOL03) vs orchestration/blueprints/blazor-page-item.yaml:32,51 vs tools/run-tests.ps1:82-83 | Contradiction sur QUI exécute les E2E. Le blueprint dit « E2E … run by run-tests » et le script actuel les inclut (filtre `Category!=Staging&Category!=Sandbox` — E2E non exclu). Mais SOL03 demande d'« ajuster run-tests.ps1 : … Exclut Category=Staging, Category=Sandbox, **Category=E2E** (suites séparées) ». Si SOL03 est exécuté tel qu'écrit, les E2E ne seront plus jamais exécutés par aucun nœud du pipeline d'orchestration : les items WEB écriront des tests E2E jamais lancés = faux vert structurel (exactement la classe d'erreur que la règle « un test écrit mais jamais exécuté est un faux vert » interdit). | Trancher et aligner les trois : soit les E2E restent dans run-tests (corriger SOL03), soit ils sont une suite séparée et le blueprint `blazor-page-item` doit déclarer un nœud dédié qui l'exécute. Vérifier au passage la cohérence avec la CI (Stratum désactive les E2E en CI).

[P1] | orchestration/prompt.md:7 vs orchestration/protocol.md:109-115 | Le point d'entrée de l'orchestration (`prompt.md`) dit : « Read $ORCH_REPO/state.yaml for current progress (**create it if absent**) ». Le protocole dit l'exact inverse : « If state.yaml does not exist: **STOP — EXIT 1** … **Never recreate it** (recreating it would resurrect items already done and purged — absent = done) ». Un agent qui suit prompt.md avec un `$ORCH_REPO` mal configuré recréera un state.yaml « tout pending » et re-exécutera des items déjà terminés et purgés — corruption du backlog. | Corriger prompt.md : « (must already exist — if absent, EXIT 1 per protocol.md Step 1; never recreate it) ».

## Findings P2

### Backlog : dépendances et périmètre des items

[P2] | orchestration/manifest.yaml:122,124 (TRK01, TRK03) | Dépendances intra-segment non déclarées vers PIV04 : l'acceptance de TRK01 exige « Câblage IDocumentIntake (PIV04) : un push agent crée des documents Detected » (TRK.yaml:42) et TRK03 consomme l'événement `AlterationDetected` publié par PIV04 (TRK.yaml:96-98,102), mais le manifest déclare TRK01 `depends_on: [PIV01]` et TRK03 `depends_on: [TRK02]`. En multi-agents, TRK01 peut être claimed pendant que PIV04 est in_progress (les deux sont éligibles dès PIV01 done) : l'interface à câbler n'existera pas dans la branche → item bloqué ou conflit. C'est le pattern de la leçon v4 n°4 (« vérifier l'ordonnancement par les consommateurs »). | Ajouter `PIV04` aux `depends_on` de TRK01 et TRK03 dans le manifest.

[P2] | orchestration/manifest.yaml:202 (OPS03) | OPS03 (écran Blazor de provisioning de tenant) déclare `depends_on: []` alors qu'il consomme WEB01 (shell/navigation), CFG02 (ImportTenantSeed), PIV05 (RegisterAgent) et la permission supervision. C'est couvert par les gates amont (deploiement-toolkit dépend de GATE_CONSOLE_WEB), donc non bloquant, mais le `depends_on` vide est trompeur pour un item d'écran. | Ajouter un commentaire dans OPS.yaml listant les prérequis inter-segments garantis par GATE_CONSOLE_WEB (convention : les `depends_on` ne traversent pas les segments).

[P2] | orchestration/items/PIV.yaml:117-118 (PIV04) | Fausse alternative d'ADR : PIV04 propose de stocker les PDF reçus via « module Document du socle Stratum OU stockage fichier par tenant — choix documenté par ADR ». Or le module Document n'est PAS dans la liste de vendoring de SOL01 (SOL.yaml:21-24 — seuls Identity/Job/Notification/Audit sont copiés) : la première option n'existe pas sans modifier le périmètre de SOL01. | Corriger PIV04 : soit retirer l'option « module Document du socle », soit ajouter explicitement ce module à la liste de vendoring de SOL01.

### Specs et contenu fonctionnel

[P2] | orchestration/items/VAL.yaml:110-121 (VAL05) vs docs/conception/F01-F02 (IsCompanyHint « heuristique adaptateur ») | Frontière ambiguë : F01-F02 définit `IsCompanyHint` comme une heuristique calculée par l'ADAPTATEUR (côté agent), tandis que VAL05 place toute l'heuristique (raison sociale, n° TVA, regex formes juridiques) dans le module Validation (plateforme). Aucun item ADP ne demande le calcul, donc pas de violation actuelle — mais un agent d'orchestration qui lit F01-F02 en premier pourrait implémenter la regex côté adaptateur (logique métier dans l'agent = P1 en review). | Amender F01-F02 : `IsCompanyHint` côté agent = transcription brute d'un champ source (`societe` non vide), TOUTE heuristique (formes juridiques, décision de blocage) = VAL05 plateforme. Le préciser aussi dans ADP02/ADP03.

[P2] | orchestration/items/TRK.yaml:184-224 (TRK06) + API.yaml (API03) | La commande v5 `verify-archive` (CLI01 : vérification d'intégrité de toute la chaîne d'archive, déclenchable à la demande par l'opérateur) n'a pas d'équivalent opérateur complet en v6 : l'ArchiveVerifier existe (TRK06) mais n'est exposé que par document (WEB03) et dans les exports (API03). Aucune action « vérifier l'intégrité de tout le coffre du tenant maintenant ». | Ajouter un critère à API03 ou WEB04 : endpoint/bouton « Vérifier l'intégrité de l'archive » exécutant l'ArchiveVerifier sur tout le tenant avec affichage du rapport.

[P2] | orchestration/items/TVA.yaml (TVA04) vs PIP.yaml:18-23 (PIP01) | Le critère d'acceptation de TVA04 « le moteur refuse l'envoi en production avec une table marquée exemple » risque le faux vert : le garde-fou réel (croisement environnement du compte PA × état de la table) vit dans PIP01, pas dans TVA04. Un agent peut « valider » TVA04 avec un test unitaire qui ne teste pas le couplage réel. | Préciser dans TVA04 que ce critère est prouvé par le test d'intégration du garde-fou PIP01 (le référencer), ou reformuler le critère TVA04 sur ce que le module TvaMapping porte réellement (le marqueur d'exemple).

[P2] | docs/conception/F05-Client-API-B2Brouter.md §4.2/§8 vs orchestration/items/TRK.yaml:67-72 (TRK02) | La mécanique de renvoi après rejet du backlog (supersede : le logiciel source est le seul créateur de numéros) remplace le « suffixe -R1 » de F05 — choix correct, mais F05 n'est pas amendée et decisions.md ne consigne pas l'abandon du suffixe. Un développeur lisant F05 pourrait le réintroduire. | Ajouter une note d'amendement en tête de F05 (ou dans §4.2/§8) renvoyant à la mécanique supersede de TRK02/F06.

[P2] | docs/conception/F05-Client-API-B2Brouter.md §3 vs orchestration/items/PAA.yaml:17-33 (PAA01) | `LookupDirectoryAsync` (annuaire) prévu par F05 dans IPaClient n'a pas été repris dans PAA01 (9 méthodes, pas d'annuaire). Défendable en V1 (B2C, pas de routage SIREN), mais l'écart spec→backlog n'est pas documenté. | Noter dans PAA01 ou F05 que l'annuaire est différé en phase 2 (B2B/routage).

[P2] | docs/conception/F04 §3.1 + F05 §2 vs lots VAL/SUP/PIP | Le diagnostic « SIREN publié / tax_report_setting actif côté PA » (Warning F04, piège staging connu « Transport not available » : start_date dans le futur = aucun envoi possible) n'est câblé dans aucun item — PAB03 implémente Get/EnsureTaxReportSetting mais personne ne déclenche le diagnostic ni l'alerte. | Ajouter ce diagnostic comme règle d'alerte SUP01 ou contrôle pré-send PIP01, tracé vers F04 §3.1.

[P2] | docs/market/Offre-Editeur-Passerelle.md (réception via portail PA) vs orchestration/items/DOC.yaml (DOC01) + CMP.yaml (CMP03) | L'engagement « la réception fournisseurs est couverte par le portail de la PA en attendant la phase 2 » (décision 2026-06-02) n'apparaît ni dans le guide éditeur (DOC01) ni dans le scénario d'objections de la démo (CMP03). Risque : laisser croire que le produit couvre la réception. | Ajouter une ligne sur le périmètre réception V1 dans DOC01 et CMP03.

[P2] | docs/conception/F06-Tracking-Piste-Audit.md (corps §8) + docs/market/Cadrage_Passerelle…:126,341 vs orchestration/items/TRK.yaml:137-224 | Contradiction documentaire résiduelle : le corps de F06 et le Cadrage disent encore « archivage à valeur probante délégué à la PA », alors que la décision 2026-06-02 et TRK05/TRK06 intègrent le coffre WORM au produit. L'amendement de F06 couvre PostgreSQL mais pas ce point. | Marquer F06 §8 et les passages du Cadrage comme caducs (renvoi vers l'amendement/TRK05-06) pour éviter toute promesse commerciale contradictoire.

[P2] | orchestration/items/PIP.yaml:84-127 (PIP03) | La transmission d'une période déclarative SANS encaissement (déclaration « néant ») n'est traitée nulle part — ni comportement, ni décision ouverte. F09 ne tranche pas ce point. | Ajouter à PIP03 une décision ouverte « déclaration néant : obligatoire ? — à confirmer DGFiP/PA » avec comportement par défaut sûr (ne rien transmettre + signaler).

[P2] | orchestration/items/OPS.yaml:63-89 (OPS03) + WEB.yaml | La gestion CONTINUE des utilisateurs et rôles (ajouter un 2e utilisateur, retirer un droit, désactiver un compte) n'apparaît dans aucun item : seul le « premier utilisateur » (OPS03) est couvert. Probablement délégué aux écrans du module Identity vendored, mais aucun item ne le dit ni ne le teste. | Ajouter une ligne explicite (WEB01 navigation ou OPS03) : la gestion utilisateurs/rôles passe par les écrans Identity du socle, avec un test E2E « ajout d'un 2e utilisateur + retrait de droit ».

[P2] | orchestration/items/PIP.yaml:26-31,48 (PIP01) | La reprise des documents restés en état `Sending` est décrite « au prochain traitement » (job planifié) ; le cas « crash plateforme → reprise au redémarrage » n'est qu'implicite. | Ajouter à l'acceptance PIP01 : « les documents en Sending sont repris dès le premier cycle après redémarrage de la plateforme (testé) ».

### Faisabilité technique

[P2] | orchestration/items/PIV.yaml:50-66 (PIV02) | L'approche de canonicalisation est la bonne (writer JSON manuel dans Agent.Contracts), mais les règles qui font réellement diverger Newtonsoft (net48) et System.Text.Json (.NET 10) ne sont pas figées : format d'émission des decimals (1.0 vs 1.00 vs 1), table d'échappement Unicode (STJ échappe beaucoup plus par défaut), format de date exact. Deux implémentations « canoniques » indépendantes peuvent diverger. | Figer dans PIV02 (ou un ADR) : format decimal imposé, échappement ASCII-only (\uXXXX pour tout non-ASCII), format de date unique, et la règle « UN writer manuel partagé, jamais deux sérialiseurs configurés pareil ».

[P2] | orchestration/items/SOL.yaml:11-54 (SOL01) | Volume de vendoring élevé pour une session : ~50 800 LOC C#, 33 projets (Common 485 fichiers, Identity 164, Notification 198, Job 78, Audit 64, Host 59), plus l'adaptation du Host (`AppBootstrap.cs` ~900 lignes qui enregistre en dur 21 modules ERP à élaguer, et toute la sécurité Keycloak/tenancy/PermissionPolicyProvider vit dans le Host, pas dans Identity). Le périmètre de review est bien borné (adaptations seulement) mais le budget contexte d'une session est incertain, et l'acceptance « les tests du socle vendored passent » exige Docker/Testcontainers. | Évaluer un découpage de SOL01 (vendoring brut compilable / Host adapté + docker-compose + ADR) ou prévoir une copie scriptée hors-LLM suivie d'une adaptation LLM. Confirmer la disponibilité Docker.

[P2] | orchestration/items/AGT.yaml:134-158 (AGT04) | Auto-update côté agent : l'approche (updater séparé) est bonne mais les points durs Windows/net48 ne sont pas tranchés — compte de service et droits (arrêt du service, remplacement des binaires), verrou de fichiers (impossible de remplacer un binaire chargé), survie de l'updater pour le rollback si la nouvelle version ne démarre pas. | ADR AGT04 : compte de service (LocalSystem ?), emplacement des binaires, updater en process détaché, détection « heartbeat sain sinon rollback » avec timeout.

[P2] | orchestration/items/OPS.yaml:55-61 (OPS02) | La migration DbUp lors d'une mise à jour de plateforme n'est décrite qu'au singulier (« migration DbUp automatique au démarrage »). Avec database-per-tenant, une montée de version doit migrer TOUTES les bases tenant + la base système, avec gestion d'échec partiel (tenant 7/20 échoue → état mixte). Aucun item ne décrit cette orchestration ni la granularité des sauvegardes (par base tenant vs pg_dump global — nécessaire à la réversibilité OPS06). | Compléter OPS02 (ou item dédié) : boucle de migration multi-bases, comportement sur échec partiel, blocage des push pendant migration ; trancher la granularité des sauvegardes dans OPS01.

[P2] | orchestration/items/OPS.yaml:8-37 (OPS01) | L'hypothèse de coût des instances hébergées (RAM ~4 GB cible avec Host .NET 10 + PostgreSQL + Keycloak JVM + reverse proxy) n'est pas étayée par une mesure ; l'ADR Keycloak (par instance vs realm mutualisé) est repoussé « après mesure » sans critère. | Dans l'ADR OPS01 : mesurer l'empreinte réelle de Keycloak 26 en mode prod heap-borné et documenter la RAM plancher réaliste avant de figer le pricing des instances hébergées (DR9).

[P2] | orchestration/items/OPS.yaml:122-149 (OPS05) | Le critère de choix x86/x64 à l'installation de l'agent n'est pas écrit. Un service installé en x64 ne peut pas charger le driver ODBC Pervasive 32-bit ; l'installeur « choisit » mais sur quel critère ? | Préciser dans OPS05/installation-agent.md : x86 par défaut dès qu'un adaptateur ODBC 32-bit est requis ; le service et ses adaptateurs partagent le même bitness.

[P2] | orchestration/items/AGT.yaml:18-25 (AGT01) + AGT.yaml:173-174 (AGT05) | Le buffer SQLite WAL est partagé entre le service Windows (LocalSystem) et le CLI (run manuel, utilisateur différent) : WAL multi-process exige des ACL d'écriture communes sur les fichiers -wal/-shm, et le « même verrou local » n'est pas défini. | Définir dans AGT01/AGT05 : mutex nommé Windows pour le verrou inter-process, ACL de `ProgramData\Liakont` permettant service ET CLI, ou architecture où le CLI sollicite le service au lieu d'ouvrir la base.

[P2] | orchestration/items/OPS.yaml:8-37 (OPS01) | La restauration des sauvegardes est « testée » au niveau script, mais aucun critère n'exige une restauration de bout en bout prouvée : restore d'un dump dans une instance vierge → données + coffre WORM intègres → ArchiveVerifier (TRK06) vert post-restore. Une sauvegarde jamais restaurée est un faux vert classique — et l'opérateur héberge des archives fiscales de tiers (10 ans). | Ajouter ce test de restauration complet à l'acceptance OPS01.

[P2] | orchestration/items/OPS.yaml (lot OPS) | Aucun item ne couvre le PRA : panne de la plateforme = tous les tenants de l'instance bloqués simultanément à l'approche d'une échéance déclarative. Pas de RTO/RPO cible, pas de procédure de reprise testée. L'analyse d'impact (§7) identifie ce risque mais aucun item ne le matérialise. | Ajouter un critère OPS (ou item dédié) : RTO/RPO documentés, procédure de reprise testée, lien avec la méta-supervision OPS04.

### Socle Stratum (constats sur pièce à intégrer au vendoring)

[P2] | orchestration/items/PIV.yaml:148-152 (PIV05) vs C:\Source\Stratum (table notification.api_keys, V012) | Le modèle ApiKey du socle (prefix+hash, scopes, expiration) est réutilisable comme PATTERN mais pas comme table : `api_keys` est clé sur `company_id` (uuid), pas sur le TenantId (string), et il n'existe AUCUN middleware de validation de clé entrante dans Stratum. PIV05 prévoit bien une entité `Agent` propre et le middleware — cohérent — mais la formulation « suit celui du socle Stratum (Notification.ApiKey) » peut induire une réutilisation de la table. | Clarifier dans PIV05 : réutilisation du pattern uniquement, entité Agent propre au module Ingestion, scopée tenant.

[P2] | orchestration/items/SOL.yaml:23 (SOL01) vs C:\Source\Stratum migrations V009/V010 du module Notification | Le module Notification embarque des migrations qui seedent des données métier ERP (templates `reservation-ack`, règles de routage réservation) dans chaque base de tenant. Copié tel quel, chaque tenant Liakont reçoit des templates de réservation de mairie. | Lors du vendoring (SOL01), neutraliser V009/V010 et consigner l'écart dans la provenance.

[P2] | orchestration/items/SOL.yaml + tous les items créant un module | Les tests d'architecture Stratum (`ModuleIsolationTests.cs:84-139`) exigent que CHAQUE dossier sous `src/Modules/` contienne `MODULE.md`, `INVARIANTS.md` et `SCENARIOS.md`, sinon verify-fast échoue. Aucun item Liakont créant un module (PIV04, TRK01, TVA01, VAL01, PAA01…) ne mentionne ces trois fichiers. | Documenter cette obligation dans SOL04 (module-rules.md) et/ou dans le blueprint module-work-item ; sinon adapter ModuleIsolationTests à la copie (consigné dans la provenance).

[P2] | C:\Source\Stratum\docs\adr (ADR-0010 dupliqué) | Stratum contient deux ADR portant le numéro 0010 (schema-per-tenant ET github-issue-reporter), et ADR-0010 (schema-per-tenant, « Accepted ») coexiste avec ADR-0011 (database-per-tenant, « Accepted », implémenté). Le code EST database-per-tenant (pas de blocage), mais un agent qui suit une référence « ADR-0010 » peut tomber sur le mauvais document. | Lors de la copie des ADR du socle (SOL01), dédupliquer le numéro 0010 et faire pointer toutes les références multi-tenancy vers ADR-0011.

### Outillage et documents d'orchestration

[P2] | tools/codex-review.ps1:51-65 (Register-Findings) | Une sortie de reviewer vide ou non conforme au format (`[P1] |`/`[P2] |`) avec exit 0 est comptée comme « CLEAN » : aucun garde-fou ne vérifie que la sortie ressemble à une review (ni sentinelle « No findings. », ni au moins un marqueur). Même classe de faux vert que l'incident historique de ce script. | Exiger qu'une review clean contienne la sentinelle « No findings. » (ou équivalent) ; toute sortie sans marqueur ET sans sentinelle = échec moteur, pas clean.

[P2] | tools/codex-review.ps1:153-157 (Get-BaseDiff) | Une `-Base` inexistante (ex. `feat/socle` au lieu de `feat/socle-v6`) produit un diff vide silencieux (`git diff` exit 128 ignoré via `2>$null`), indistinguable d'un diff légitimement vide → exit 3 avec un message qui ne pointe pas la vraie cause. | Vérifier `git rev-parse --verify $Ref` avant le diff et échouer avec « base ref introuvable ».

[P2] | tools/build-agent-context.ps1:49-67 | La table `$lotToSpecs` est désynchronisée du manifest v6 : les lots AGT, WEB, SUP, OPS, BRD (23 items) n'ont aucun mapping de specs (pas de F12 pour l'agent, pas de F10 pour le web), et les lots v5 supprimés SVC, CLI, WPF y figurent encore. Contexte dégradé pour près d'un tiers des items. | Ajouter AGT→F12, WEB/SUP→F10+F12, OPS/BRD→F12 §6/blueprint §3.3 ; supprimer SVC/CLI/WPF.

[P2] | docs/architecture/definition-of-done.md:18,21-25 | La DoD (référencée par protocol.md comme document autoritaire) est restée à l'état v5 : section « items WPF (wpf-screen-item) » avec ViewModel + checklists smoke (blueprint supprimé, pratique abandonnée), « frontière Core/Adaptateur » (vocabulaire v5), et AUCUNE section pour `blazor-page-item` (bUnit/Playwright/libellés FR) ni pour le double build. SOL03 prévoit de la mettre à jour mais seulement « avec les commandes réelles ». | Réécrire la DoD pour v6 (section Blazor, frontières modules/agent, double build) — soit immédiatement, soit en élargissant explicitement le périmètre de SOL03.

[P2] | docs/architecture/orchestration-protocol.md:55-86 | Le diagramme décrit le manifest v2 : segments `service-api`/`console-admin`, gates `GATE_SERVICE_API`/`GATE_CONSOLE_ADMIN`, « WPF cliente de l'API » — tous inexistants en v6. Document d'orientation trompeur pour un agent. | Régénérer le diagramme depuis le manifest v6 (ou supprimer le document s'il fait doublon avec protocol.md).

[P2] | orchestration/protocol.md:14 + README.md:29,102 | Références « F01-F11 » alors que F12 existe et est LA spec d'architecture du pivot — précisément celle qu'un agent d'orchestration doit lire. | Remplacer par « F01-F12 ».

[P2] | orchestration/MANIFEST-CONVENTIONS.md:46 | La liste des blueprints non-défaut cite `wpf-screen-item`, blueprint v5 supprimé (remplacé par `blazor-page-item`). | Remplacer par `blazor-page-item`.

[P2] | tools/verify-fast.ps1:46 (Run-Step) | `$LASTEXITCODE` périmé : un step purement PowerShell hérite du `$LASTEXITCODE` de la dernière commande native exécutée et peut échouer à tort. Non déclenché aujourd'hui (les steps PS purs tournent avant tout dotnet) mais fragile au moindre réordonnancement. | Réinitialiser `$global:LASTEXITCODE = 0` avant chaque step, ou ne tester l'exit code que pour les steps invoquant un exécutable natif.

[P2] | orchestration/protocol.md:347 + .claude/agents/orch-fix-review.md:26 + orch-fix-verify.md:25-26 + tools/run-tests.ps1:17 | Références vers des fichiers inexistants (`docs/architecture/repo-standards.md`, `module-rules.md`, `testing-strategy.md`, `provenance-socle-stratum.md`) — livrables v5 invalidés, à recréer par SOL01/SOL04. Dégradation gracieuse (les agents tolèrent un fichier absent), mais renvois morts pendant tout le segment socle. | Annoter ces renvois « (créé par SOL01/SOL04) » et vérifier que SOL04 recrée bien les trois documents.

[P2] | orchestration/items/AGT.yaml:141 + OPS.yaml:25 + TRK.yaml:141 | Citations « F12 §7.4 », « §7.1 », « §7.2 » : la section 7 de F12 est un tableau de décisions sans sous-sections numérotées. Contenu traçable (lignes 1/2/4 du tableau) mais forme imprécise. | Écrire « F12 §7, décision n°X » (style déjà utilisé dans AGT.yaml:115).

[P2] | orchestration/items/VAL.yaml:5 | Citation « règle blueprint §2.5 » : le blueprint n'a pas de §2.5 ; c'est la règle 5 de la liste du §2. | Écrire « blueprint §2 règle 5 » (style de PAA.yaml:70).

[P2] | orchestration/protocol.md:168-169 | L'exemple de la convention tiret-vs-slash utilise encore le nom de branche v5 supprimé `feat/socle`/`feat/socle/SOL01`. Sans impact d'exécution (la dérivation est dynamique), mais induit en erreur. | Mettre l'exemple à jour (`feat/socle-v6` ou un segment neutre).

## Dimensions sans finding

- **Dimension A (cohérence structurelle)** : aucun P1. Les 91 entrées manifest ↔ state.yaml ↔ fichiers de lots sont parfaitement alignées, zéro cycle, zéro item orphelin, zéro référence cassée vers un item/blueprint/gate, priorités cohérentes, renumérotation TRK v6 intègre dans toutes les références croisées, encodages 100 % conformes (BOM sur les .ps1, pas de BOM sur .md/.yaml), branche `feat/socle-v6` propre.
- **Dimension B (alignement doctrine)** : aucun P1. Aucune donnée client dans les items produit, aucune dépendance à un PA concret, frontières de modules et agent/plateforme respectées dans tous les items, tenant-scoping systématique, lot CMP = paramétrage pur.
- **Dimension I (fidélité v5→v6)** : aucun P1. Aucune perte de contenu fiscal — test-espion lecture seule (ADP02), exception La Poste (VAL02), prorata (PIP03), chaînage des addenda (TRK05), 3 familles d'erreurs (PAB02), idempotence rectificatifs (PIP04), machine à états + supersede + ManuallyHandled (TRK02), heuristique B2B (VAL05), garde production (PIP01), toutes les leçons des reviews v4/v5 : présents et conformes.

## Questions ouvertes (ni P1 ni P2 — à trancher par un humain)

1. **GATE_DEMO_ISATECH** (manifest.yaml:218) est une gate intra-segment qui n'est la `gate:` d'aucun segment : au passage `gate_pending`, le protocole (Step 2) ne crée une PR que pour les gates de segment — cette gate restera en attente sans PR ni notification formelle. Intentionnel (checkpoint humain sans PR) ou trou de câblage ?
2. **Stratégie « jobs par tenant »** (lié au P1 module Job) : item socle dédié dont dépendraient PIP01/PIP03/SUP01/TRK06, ou critère du premier consommateur (PIP01) ? À décider avant le segment pipeline.
3. **Identity ↔ Party** (lié au P1 vendoring) : vendoriser Party.Contracts ou patcher Identity ? La décision change le périmètre exact de SOL01.
4. **Découpage** : SOL01 (~50 800 LOC) et le segment core-foundation (24 items, une seule gate, goulot du chemin critique vers septembre 2026) gagneraient-ils à être scindés pour un retour humain plus tôt ?
5. **Topologie CMP** (tenant mutualisé / instance dédiée / appliance on-premise) : décision ISATECH attendue — dimensionne CMP04 et l'urgence d'OPS01 (déjà tracée dans blueprint §12).
6. **AA/AAA** : existe-t-il un cas légitime de catégorie AA/AAA à taux 0 ? À confirmer avec l'expert-comptable avant de figer la règle VAL04 étendue.
7. **Déclaration néant e-reporting** : la transmission d'une période sans encaissement est-elle obligatoire (fiches DGFiP) ? Non tranché dans F09.
8. **Séquestre APP** : engagement commercial (Offre Éditeur) sans item ni décision — confirmé comme dispositif purement contractuel hors backlog produit ?
9. **Échéances déclaratives** (lié au P1 CFG) : si la décision produit est de ne PAS calculer les échéances en V1, retirer l'alerte de SUP01/WEB01 plutôt qu'ajouter le paramètre.
10. **Anti-doublon après réinstallation d'agent** : un agent réinstallé (perte du pushed_log local, rétention 90 j) re-pousse tout ; l'anti-doublon plateforme (payload_hash par tenant) couvre ce cas mais ce n'est tracé comme critère nulle part. À confirmer au plan de PIV04/AGT02.
11. **OPS02 collecte le branding mais s'exécute avant BRD01** (priorités 7010 < 7060, pas de dépendance) : provisioning avec branding par défaut puis enrichi, ou OPS02 doit-il dépendre de BRD01 ?
12. **Commandes POSIX dans le protocole** (`&&`, `rm` — protocol.md:200,265,299) : valides seulement si l'orchestrateur les exécute via l'outil Bash. À confirmer ou réécrire en syntaxe neutre.
13. **Résidus v5 dans le dépôt d'état** (`events.jsonl` : 12 événements SOL01-03/Gateway.sln/PR #1 ; `session-log/` : 4 logs v5) : trace historique voulue, ou à archiver dans `$ORCH_REPO/archive/` pour ne pas polluer l'audit v6 ?

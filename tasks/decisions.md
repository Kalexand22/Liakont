# Journal des décisions

_Décisions de pilotage prises en session interactive (les décisions d'architecture vont dans
docs/adr/, les décisions fiscales appartiennent à l'expert-comptable du client concerné)._

| Date | Décision | Justification |
|---|---|---|
| 2026-06-02 | Reprise du principe d'orchestration Stratum pour Conformat | Système éprouvé (85+ sessions, ~100 items livrés sur Stratum) |
| 2026-06-02 | Dépôt d'état séparé : C:\Source\conformat-orchestration | Éviter les conflits de merge, permettre le multi-agents |
| 2026-06-02 | Pas de Playwright (produit WPF desktop) : tests ViewModel + checklists smoke | Adaptation du principe « UI = E2E obligatoire » de Stratum au desktop |
| 2026-06-02 | Sous-branches nommées `<segment>-<item>` (tiret, jamais slash) | Collision de namespace de refs git détectée par la 1ère session d'orchestration |
| 2026-06-02 | **Conformat est un produit GÉNÉRIQUE** : 2 axes de plug-ins (sources IExtractor + PA IPaClient), toute donnée client = paramétrage dans deployments/ | Correction d'alignement utilisateur — le backlog v1 mettait la table TVA CMP dans le Core (erreur) |
| 2026-06-02 | Multi-PA dès la V1 : plug-ins B2Brouter ET Super PDP, abstraction + capacités + tests de contrat | Super PDP (superpdp.tech) porte l'Offre Éco marque grise (DR17/DR9) — exigence commerciale, pas du futur-proofing |
| 2026-06-02 | Aucune fonctionnalité produit ne dépend d'UN PA : comportement piloté par PaCapabilities | « On ne va rien bloquer parce qu'1 PA n'est pas prêt » (F09 développé à fond) |
| 2026-06-02 | **Architecture multi-utilisateurs : le Service est le cœur** (ordonnanceur + pipeline + API HTTP + seul accès SQLite), la console WPF est cliente HTTP | Besoin : plusieurs comptables vérifient depuis leurs postes ; SQLite sur partage réseau = corruption |
| 2026-06-02 | Stockage Tracking multi-provider : SQLite (défaut) + SQL Server Express (politiques IT), via Dapper | Institutions (type CMP) imposent souvent « toutes les bases sur NOTRE SQL Server » |
| 2026-06-02 | **Archivage fiscal 10 ans intégré au produit** : coffre local WORM + chaîne de hashes + rendu lisible ; copie PA en doublon | L'offre vend « archivage 10 ans inclus » ; l'archive doit être indépendante de la PA. Limite assumée : pas de certification NF Z42-013 |
| 2026-06-02 | **Ancrage temporel via abstraction ITimestampAnchor** : OpenTimestamps (défaut, gratuit, ancrage blockchain Bitcoin) + RFC 3161 qualifié eIDAS (option premium ~15-50 €/an) + NoAnchor | Coût récurrent nul par défaut ; la présomption eIDAS reste disponible en option pour qui la veut. Même principe de généricité que les plug-ins PA |
| 2026-06-02 | **Le coffre d'archive contient les VRAIES factures** quand les capacités le permettent : Factur-X générée par la PA (capacité SupportsDocumentRetrieval) + bordereau PDF du logiciel source (capacité ProvidesSourceDocuments de l'adaptateur — confirmé accessible pour EncheresV6) | Sans ça, le coffre n'archivait que les données transmises + preuves (piste d'audit). Avec, l'argument « archivage 10 ans » couvre les factures elles-mêmes |
| 2026-06-02 | Objectif du backlog : socle produit final (GATE_TOOLKIT) → démo ISATECH (GATE_DEMO_ISATECH) → production CMP (GATE_PROD_CMP) | La démo et la prod CMP sont des applications du produit, pas le produit |
| 2026-06-02 | Stack : .NET 4.8, SDK-style csproj, xUnit, MahApps.Metro | Validé utilisateur |
| 2026-06-02 | **Paramétrage comptable éditable depuis la console** (périmètre : table TVA UNIQUEMENT) : édition journalisée (qui/quand/avant/après), toute modification invalide la validation → revalidation expert-comptable requise. La config TECHNIQUE (ODBC, comptes PA, planification) reste en fichiers côté serveur. Items : TVA05, API04, WPF07 + 3ème niveau de droits « paramétrage » | Le comptable doit pouvoir compléter la table TVA quand un régime non mappé bloque des documents, sans intervention IT. Validé utilisateur |
| 2026-06-02 | **Réconciliation PDF ↔ documents émis** : nouvelle capacité `ProvidesUnlinkedDocumentPool` (pool de PDF sans lien explicite) + moteur de réconciliation — lien AUTO seulement en confiance haute (n° de document dans le nom de fichier ou le texte du PDF), file d'attente manuelle sinon. Items : TRK08, API04, WPF08 + PIV03/ADP05 enrichis | Certains logiciels source déposent tous leurs PDF au même endroit sans lien explicite. Un mauvais rapprochement archivé en WORM serait incorrigible → jamais d'auto en confiance moyenne. Validé utilisateur |
| 2026-06-02 | **Review indépendante du backlog v4 traitée → manifest v5** : 28 P1 + 19 P2, 46 findings confirmés sur pièce, 1 contesté (CMP04 — la gate humaine vérifie déjà les prérequis cochés). Corrections : faux vert codex-review, machine à états orch-state, blueprint WPF (frontière API + ordre des nœuds), specs F10/F11/F04/F01-F02 amendées, câblages pipeline (anti-doublon timeout, PDF, altération), piste d'audit paiements, chaînage addenda, rectificatifs RE (PIP04), actions API/console (resend, resolve-manually, export période) | Une review indépendante AVANT le lancement de l'orchestration coûte 1 session et a détecté des faux verts d'outillage et des inversions de dépendances qui auraient bloqué les agents |
| 2026-06-02 | **Reviewer automatique : Claude reste primaire, Codex en repli** (statu quo confirmé) | Décision utilisateur sur la question ouverte de la 2ème review. La gate humaine reste le filet de sécurité : l'IA ne merge jamais dans main |
| 2026-06-02 | **Pas de jalon de démo intermédiaire avant GATE_TOOLKIT** : la démo ISATECH reste une application du produit complet packagé | Décision utilisateur. Le risque d'ordonnancement vers l'échéance de septembre 2026 est assumé consciemment — cohérent avec « la démo et la prod CMP sont des applications du produit, pas le produit » |
| 2026-06-02 | **2ème review indépendante traitée** (2 P1 + 23 P2) : (a) faux vert majeur — le nœud review des blueprints s'exécutait APRÈS commit, l'arbre étant propre codex-review ne revoyait RIEN → désormais `-Base <segment-branch>` obligatoire + exit 3 si rien n'est revu ; (b) machine à états complétée — état terminal `ManuallyHandled` (motif obligatoire) + mécanique de remplacement après rejet clarifiée (le logiciel source est le seul créateur de numéros : l'opérateur LIE le document rejeté à son remplaçant ré-extrait, action `supersede`) ; + endpoint `recheck`, exceptions typées IExtractor, reprise crash (état Sending), heuristique B2B alignée sur la spec, SQLite.Interop par plateforme, contraintes net48 (RFC 3161, parseur PDF, licences AGPL interdites) documentées dans les ADR à produire | Deux reviews indépendantes valent mieux qu'une : la 2ème a trouvé un faux vert que la 1ère avait raté (review post-commit sans -Base) |
| 2026-06-02 | **Offre commerciale réalignée sur le produit V1** : réception fournisseurs + B2B international (Flux 10.1) = phase 2 (réception couverte en attendant par le portail PA inclus) ; la génération Factur-X est faite par la PA, jamais promise comme locale | Ne vendre QUE ce que le produit fait. Validé utilisateur (option « corriger l'offre » retenue contre « ajouter les lots ») |
| 2026-06-02 | **GATE_TOOLKIT dépend de GATE_PA_B2BROUTER + GATE_ADAPTER_ENCHERESV6, PAS de GATE_PA_SUPERPDP** | Le produit packagé inclut au moins 1 PA réel + 1 adaptateur réel ; Super PDP rejoint le package dès que sa gate passe sans bloquer la release (sa sandbox dépend d'un tiers). Validé utilisateur |
| 2026-06-02 | **Cohérence catégorie TVA / taux = BLOCKING** (S→taux>0, E/Z/AE/G/K/O→taux=0) — amendement de F04 §3.4 qui la classait Warning | « Bloquer plutôt qu'envoyer faux » : une incohérence = erreur de mapping ou donnée source fausse. Validé utilisateur |
| 2026-06-02 | **Multi-provider stockage CONFIRMÉ** (SQLite défaut + SQL Server Express option) ; la phrase contradictoire du blueprint (« SQLite suffit définitivement ») est corrigée | Validé utilisateur — la décision du 2026-06-02 (DSI exigeantes) tient |
| 2026-06-02 | **Configuration (CFG01/CFG02) déplacée en core-foundation ; packaging = nouveau lot PKG (deploiement-toolkit)** | Inversion de dépendances détectée par la review : SVC01/API01/CLI01 consomment GatewayConfig qui était ordonné APRÈS eux |
| 2026-06-02 | **FakePaClient = plug-in à part entière (`Gateway.PaClients.Fake`), retiré du Core** | Le Core ne contient QUE l'abstraction (blueprint §2 règle 4) ; le plug-in Fake livré prouve le modèle plug-in et sert de PA de démo hors-ligne |
| 2026-06-03 | **PIVOT D'ARCHITECTURE : plateforme web centralisée multi-tenant (socle Stratum, .NET 10, PostgreSQL, Blazor) + agent local léger (net48/x86)** — remplace l'architecture on-premise (service Windows + console WPF + tracking SQLite chez chaque client). L'agent = extraction ODBC + pool PDF + buffer SQLite + push HTTPS + heartbeat, seul composant chez le client final. La plateforme = pivot, TVA, validation, tracking, archivage, envoi PA, console web, supervision. Analyse complète : tasks/analyse-impact-pivot-plateforme.md | (1) Modèle marque grise éditeurs : 1 éditeur = N clients finaux, impossible à servir économiquement en on-premise ; (2) support proactif : la détection des pannes silencieuses (dead-man's switch) est structurellement impossible en on-premise, or l'e-reporting a des échéances légales ; (3) les points durs net48 (RFC 3161, OpenTimestamps, parsing PDF/AGPL) disparaissent ; (4) mises à jour réglementaires centralisées. Validé utilisateur |
| 2026-06-03 | **Trois topologies de déploiement du MÊME produit** : self-hosted éditeur (appliance Docker sur son infra) · instance dédiée hébergée par IT Innovations (cloisonnée, marque éditeur) · mutualisée IT Innovations (clients directs). Une instance = multi-tenant (tenant = client final). La marque grise = une instance PAR éditeur, jamais un SaaS mondial mutualisé | Profil des éditeurs cibles (« rien du tout » ↔ « serveur chez un hébergeur ») : leur réticence porte sur le CONTRÔLE et la visibilité de leur parc client, pas sur la techno. Instance par éditeur + clause de réversibilité (dump/restore/DNS) + séquestre APP = les trois objections bordées. Validé utilisateur |
| 2026-06-03 | **Pas de double architecture** : la console WPF et le tracking SQLite/SQL Server local sont abandonnés. SQLite survit uniquement comme buffer de l'agent ; DPAPI uniquement pour les secrets de l'agent ; le besoin on-premise est couvert par l'appliance single-tenant (même plateforme web déployée chez le client) | Maintenir les deux = ~2x le coût (chaque écran/feature développé deux fois) et le cœur partagé retomberait en netstandard2.0, conservant tous les points durs net48. Validé utilisateur (question « peut-on avoir les 2 ensemble ? » tranchée) |
| 2026-06-03 | **Conséquence sur le travail livré : SOL01-03 (scaffold net48, CI, docs) sont invalidés ; la PR #1 (GATE_SOCLE) ne doit PAS être mergée** ; le backlog v5 sera retravaillé en v6 | Le pivot intervient avant le démarrage du métier : perte limitée à ~2-3 sessions. Chaque semaine de plus aurait aggravé le coût du pivot |
| 2026-06-03 | **Exécution du pivot — 3 questions tranchées** : (a) PR #1 fermée sans merge, branche feat/socle abandonnée ; (b) le code plateforme vit dans le **repo Conformat** avec le **socle Stratum copié** (vendored + note de provenance, re-convergence NuGet possible plus tard) ; (c) nommage des projets : **Conformat.*** (Conformat.Host, Conformat.Modules.*, Conformat.Agent) | Validé utilisateur. Le vendoring garde tout au même endroit pour l'orchestration multi-agents — critique sous contrainte de délai ; le nommage Conformat.* porte la marque et suit le pattern Stratum.Host/Stratum.Modules.* |
| 2026-06-03 | **Reviews indépendantes v6 & v8 traitées → corrections du backlog v6 avant lancement** (24 P1 + 55 P2 dédupliqués, vérifiés sur pièce). Huit décisions actées ci-dessous (D1-D8), corrections appliquées au manifest, aux items, aux blueprints, à l'outillage et aux docs. Table de correspondance finding→correction : tasks/review-independante-v6-v8-reponses.md | Même principe que les reviews v4/v5 : une review indépendante avant lancement coûte une session et évite de bloquer les agents d'orchestration |
| 2026-06-03 | **(D1) SOL01 vendorise AUSSI Stratum.Modules.Party.Contracts** — Identity.Application le référence en dur (ProjectReference) ; Party.Contracts ne dépend que de Common.Abstractions | Sans lui le périmètre « Identity/Job/Notification/Audit seulement » ne compile pas. Copie minime, zéro modification du code Stratum. Validé utilisateur |
| 2026-06-03 | **(D2) E-reporting paiement V1 = Flux 10.4 (domestique) uniquement** ; le Flux 10.2 (international) est une capacité déclarée NON alimentée jusqu'à la phase 2 (B2B international) | Aucune règle sourcée ne permet de dériver domestique/international d'un paiement ; inventer la règle est interdit. Cohérent avec « B2B international = phase 2 ». Validé utilisateur |
| 2026-06-03 | **(D3) Tests E2E : item socle dédié (harness Testcontainers PostgreSQL + Keycloak + Playwright, adapté de Stratum) + script `tools/run-e2e.ps1` séparé** ; run-tests.ps1 exclut Category=E2E ; le blueprint blazor-page-item déclare un nœud E2E explicite qui exécute run-e2e.ps1 | Sans infra E2E, les 10 items blazor-page-item échouent ou produisent des tests jamais exécutés (faux vert structurel). La suite séparée évite de rendre run-tests dépendant de Docker/navigateurs. Validé utilisateur |
| 2026-06-03 | **(D4) Paramétrage fiscal tenant : champ `reportingFrequency` nullable ajouté à CFG01/CFG02** (décadaire/mensuelle/trimestrielle — source F09 §2). Tant qu'il est null : pas de calcul d'échéance déclarative, transmission paiement suspendue + message opérateur (même mécanique que vatOnDebits) | L'alerte « échéance J-3 » (SUP01) et l'affichage WEB01 étaient incalculables sans ce champ ; une cadence devinée = règle fiscale inventée. Validé utilisateur |
| 2026-06-03 | **(D5) VAL03 (SourceTotalsRule) : l'écart de totaux plateforme/source est un Warning** (alerte opérateur), pas un Blocking — aligné sur F04 §3.3 validé en staging | Le durcissement en Blocking du backlog n'avait pas de source ; les écarts d'arrondis légitimes de la source ne doivent pas bloquer l'envoi. Validé utilisateur |
| 2026-06-03 | **(D6) Auto-update agent V1 : manifeste de version SIGNÉ par une clé applicative dédiée (hors plateforme)**, vérifié par l'agent avant installation ; Authenticode ajouté dès qu'un certificat de signature de code est disponible. Amende la décision F12 §7 (« hash via API en V1 ») | Si la plateforme/API est compromise, un hash publié par la même API ne protège pas le code exécuté chez le client final. Le modèle de confiance est documenté par ADR. Validé utilisateur |
| 2026-06-03 | **(D7) Fin de vie d'un tenant (OPS06) : désactivation logique → export complet vérifié (ArchiveVerifier vert sur le bundle) → transfert de responsabilité de conservation documenté → suppression encadrée**, journalisée de façon irréversible au niveau instance (hors base tenant) | Concilie la résiliation/RGPD avec le coffre WORM et la conservation fiscale 10 ans : pas de suppression sans preuve que le client a récupéré une archive intègre. Validé utilisateur |
| 2026-06-03 | **(D8) Les engagements support de l'Offre Éditeur entrent au backlog** : item « kit support éditeur » (FAQ, formation N1, matrice N1/N2/N3, SLA, canal) + item « veille réglementaire » (sources, cadence, checklist d'impact) ; le périmètre réception V1 (portail PA) est ajouté à DOC01/CMP03 | L'offre commerciale engage ces livrables ; les laisser hors backlog = promesse non couverte. Validé utilisateur |

namespace Liakont.Modules.Ged.Application;

/// <summary>
/// Marqueur d'assembly pour l'enregistrement MediatR du module GED (handlers de commande et de requête, qui
/// arrivent avec GED04+ : <c>SetAxisValueCommand</c>, <c>GedMapper</c>, ingestion). Homogène avec les autres
/// modules (scaffold GED02).
/// </summary>
public interface IGedApplicationMarker;

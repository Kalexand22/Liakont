# Invariants — module Documents

| ID | Rule | Enforcement |
|---|---|---|
| INV-DOCUMENTS-001 | Les montants (`total_net`, `total_tax`, `total_gross`) sont en `decimal` exact, jamais flottant (CLAUDE.md n°1). Round-trip base ↔ decimal sans perte. Aucun montant à plus de 2 décimales n'est accepté (la colonne `numeric(18,2)` le tronquerait silencieusement — intégrité de stockage, CLAUDE.md n°4). | Colonnes `numeric(18,2)` (`V002`) ; propriétés `decimal` de `Document` ; garde-fou `RequireMonetaryScale` dans `Document.CreateDetected` ; tests `CreateDetected_RoundTrips_Decimal_Amounts_Without_Loss`, `CreateDetected_Rejects_Total*_With_More_Than_2_Decimals`, `CreateDetected_Accepts_Total*_With_Two_Or_Fewer_Decimals`. |
| INV-DOCUMENTS-002 | Le module ne calcule ni ne valide rien : il REPORTE les montants calculés par la source et le type de document BRUT (frontière module-rules §2 ; classification et contrôle des totaux délégués à Validation — ADR-0004 D3-3, F04). | `DetectedDocumentMapper.ToDetectedDocument` reporte `Totals.*` et `SourceDocumentKind` tels quels ; tests `DetectedDocumentMapperTests`. |
| INV-DOCUMENTS-003 | La création du document est IDEMPOTENTE sur l'identifiant (contrat `IDocumentIntake`) : un re-push n'ajoute ni document ni événement, et n'écrase pas un état déjà avancé. | `INSERT … ON CONFLICT (id) DO NOTHING` (`PostgresDocumentUnitOfWork.CreateDetectedAsync`, événement de genèse écrit seulement si insertion) ; tests `CreateDetected_Is_Idempotent_On_Id`, `Intake_Is_Idempotent_On_DocumentId`. |
| INV-DOCUMENTS-004 | Un document est créé en état `Detected` par l'ingestion (F06 §3, PIV04). | `Document.CreateDetected` fixe `State = Detected` ; tests `CreateDetected_Sets_State_Detected_And_Timestamps`, `Agent_Push_Creates_Detected_Document_With_Genesis_Event`. |
| INV-DOCUMENTS-005 | Tout champ source absent reste `null`, jamais un défaut implicite masquant une donnée manquante (blueprint §8). | `Document` : `supplier_siren` / `customer_name` / `pa_document_id` / `mapping_version` nullables ; `NullIfBlank` ; tests `CreateDetected_Blank_Siren_And_Customer_Become_Null`, `Maps_Null_Customer_*`, `Maps_Null_Supplier_Siren`. |
| INV-DOCUMENTS-006 | Le numéro de document n'est PAS une clé d'unicité en base : un document rejeté peut être remplacé sous un nouveau numéro, l'ancien passant `Superseded` (F06 §4). L'anti-doublon (état Issued / empreinte) est une règle APPLICATIVE (TRK03), jamais une contrainte qui casserait le remplacement. | Index NON unique `ix_documents_number` (`V002`) ; `IDocumentQueries.GetByNumberAsync` retourne le document le plus récent ; test `GetByNumber_Returns_Most_Recent_For_That_Number`. |
| INV-DOCUMENTS-007 | La piste d'audit `document_events` est APPEND-ONLY (CLAUDE.md n°4) : aucun chemin d'update/delete applicatif, et des triggers base REJETTENT tout UPDATE/DELETE d'une entrée ET tout TRUNCATE de la table — garantie indépendante du code. La table `archive_entries` est WORM (write-once) : chaque paquet ou addendum est une NOUVELLE ligne, TRK05 ne réécrit jamais une référence ; des triggers identiques (même discipline) ferment tout vecteur de mutation avant la livraison de TRK05. | `IDocumentUnitOfWork` n'expose que `AppendEventAsync` (aucun update/delete) ; triggers `trg_document_events_append_only` + `trg_document_events_no_truncate` (`V003`) + `trg_archive_entries_append_only` + `trg_archive_entries_no_truncate` (`V005`) ; tests `Update_…`, `Delete_…`, `Truncate_Of_The_Audit_Table_Is_Rejected`, `ArchiveEntry_Update_Is_Rejected`, `ArchiveEntry_Delete_Is_Rejected`. |
| INV-DOCUMENTS-008 | Isolation tenant PAR LA CONNEXION (database-per-tenant, blueprint §7) : le schéma vit dans la base DU TENANT, aucune colonne de tenant, aucune requête cross-tenant possible (CLAUDE.md n°9/17). | Lectures via `IConnectionFactory` (tenant courant, `ITenantContext`) ; ingestion via `ITenantConnectionFactory` par slug (`TenantSlugConnectionFactoryAdapter`) — la connexion est la frontière de tenant. |
| INV-DOCUMENTS-009 | La machine à états du document est EXPLICITE et FERMÉE (F06 §3, TRK02) : seules les transitions de la table autorisée sont permises ; toute autre est refusée par exception AVANT toute mutation (un état refusé n'altère jamais l'agrégat — CLAUDE.md n°3, bloquer plutôt qu'avancer un état faux). `Issued`, `Superseded` et `ManuallyHandled` n'ont aucune transition sortante. | `DocumentStateMachine.AllowedTransitions` + `EnsureCanTransition` (contrôle avant mutation) ; `Document.ApplyTransition` ; tests `DocumentStateMachineTests`, `DocumentTransitionTests.Illegal_Transition_*` / `..._States_Without_Successor_Reject_Every_Transition`, `Illegal_Transition_Leaves_Document_And_Audit_Untouched`. |
| INV-DOCUMENTS-010 | Toute transition PRODUIT automatiquement son `DocumentEvent` (on ne peut pas transiter sans le fait d'audit qui le matérialise), persisté ATOMIQUEMENT avec le nouvel état dans la MÊME transaction. États terminaux : `ManuallyHandled` (motif OBLIGATOIRE + identité opérateur) et `Superseded` (référence du remplaçant OBLIGATOIRE + identité opérateur ; la source est le seul créateur de numéros, F06 §4). | `Document.Mark*` / `Supersede` retournent le `DocumentEvent` (motif/remplaçant/opérateur exigés via `RequireText`) ; read-modify-write transactionnel `IDocumentUnitOfWork.GetForUpdateAsync` (`SELECT … FOR UPDATE`) + `UpsertDocumentAsync` + `AppendEventAsync` + `CommitAsync` ; tests `Transition_Persists_New_State_And_Audit_Event_Atomically`, `Transition_Is_Not_Visible_Until_Commit`, `Operator_Supersede_Persists_Replacement_Link_And_Operator_Identity`, `ManuallyHandled_Requires_A_Reason`, `Supersede_Requires_A_Replacement_Reference`. |
| INV-DOCUMENTS-011 | Tout document ÉMIS archive ses TROIS snapshots de preuve dans son `DocumentEvent` d'émission (payload pivot transmis, réponse PA brute, trace de mapping TVA — F06 §3, TRK04) ; tout document REJETÉ archive payload envoyé + réponse de rejet brute (la tentative ratée fait partie de la piste d'audit). Les snapshots sont OBLIGATOIRES (impossible d'enregistrer une émission/un rejet sans sa preuve) et écrits dans les colonnes jsonb append-only — jamais modifiés après coup (CLAUDE.md n°4). | Value objects `IssuanceSnapshots` (3 obligatoires) / `RejectionSnapshots` (2 obligatoires) ; `Document.MarkIssued`/`MarkRejectedByPa` exigent les snapshots et produisent l'événement via `DocumentEvent.IssuanceTransition` ; colonnes `payload_snapshot`/`pa_response_snapshot`/`mapping_trace` jsonb (`V003`) ; tests `Issued_Event_Carries_The_Three_Proof_Snapshots`, `Rejected_Event_Carries_Payload_And_Pa_Response_But_No_Mapping_Trace`, `Issuance_Snapshots_Require_*`, `Issued_Event_Persists_The_Three_Proof_Snapshots_As_Jsonb`, `Rejected_Event_Persists_Payload_And_Pa_Response_Without_Mapping_Trace`. |
| INV-DOCUMENTS-012 | L'anti-doublon AVANT envoi applique EXACTEMENT les quatre règles de F06 §4, dans l'ordre de la spec, sans en inventer aucune (CLAUDE.md n°2) : un Issued de même `(supplier_siren, document_number)` bloque (doublon) ; un `RejectedByPa` de même clé autorise le renvoi en désignant l'ancien à superséder ; une empreinte `payload_hash` identique à un Issued bloque (doublon strict) ; sinon l'envoi est autorisé. Sans SIREN, la clé fonctionnelle est incomplète et ne rapproche aucun antécédent (seul le garde-fou d'empreinte reste). La décision est PURE (testable hors base) ; le candidat est toujours exclu de ses antécédents. | Fonction pure `DocumentDuplicatePolicy.Decide` + `PostgresDuplicateDocumentCheck` (lecture tenant-scopée par la connexion, exclusion du candidat par `id <> @CandidateId`) exposés via `IDuplicateDocumentCheck` (consommé PIP01) ; tests `DocumentDuplicatePolicyTests.*` (4 règles + précédence), `AntiDuplicateIntegrationTests.Rule_42/43/44/45_*`, `..._Never_A_Duplicate_Of_Itself`, `Without_A_Supplier_Siren_*`. |
| INV-DOCUMENTS-013 | Une altération de la source détectée APRÈS émission (F06 §3) inscrit un fait d'audit `DocumentSourceAlteredAfterIssue` APPEND-ONLY sur le document émis ; le document émis n'est JAMAIS réémis ni mis à jour (DR6 point 2). Si aucun document n'est émis pour la référence, rien n'est inscrit. La consommation de l'événement d'outbox (`ingestion.source.altered`, produit par PIV04) est IDEMPOTENTE (identifiant de l'entrée = identifiant de l'événement d'intégration ; un rejeu at-least-once heurte la clé primaire). Le tenant est résolu par SLUG (worker d'outbox en scope système). | `SourceAlterationDetectedConsumer` (résolution slug via `TenantSlugConnectionFactoryAdapter`, `AppendEventAsync` seul — aucun `UpsertDocumentAsync` de l'émis, capture de la violation d'unicité au rejeu) + factory `DocumentEvent.SourceAlteredAfterIssue` (système, sans snapshot, id explicite) ; tests `DocumentEventSourceAlteredTests`, `SourceAlterationConsumerIntegrationTests.Alteration_After_Issue_*` / `..._Twice_Records_The_Alteration_Only_Once` / `..._Without_An_Issued_Document_Records_Nothing`. |
| INV-DOCUMENTS-015 (MND06) | **Re-clé anti-doublon F06 §4 pour l'autofacturation 389** (F06 §4 amendé par ADR-0025 §6, F15 §3.2/§3.3, INV-BT1-5/6) : le champ ADDITIF nullable `DuplicateCheckRequest.MandantId` sélectionne la clé. `null` ⇒ clé historique `(supplier_siren, document_number)` INCHANGÉE (aucune régression non-389). NON `null` ⇒ clé `(mandant_id, document_number)` où `document_number` = BT-1 fiscal alloué par mandant (MND05), PAS le numéro source. Deux 389 de même numéro / mandants ≠ → Send ; même mandant + ré-extraction → Blocked. Les **4 règles de F06 §4 (décision pure) sont INCHANGÉES** — seul l'ensemble d'antécédents fourni au verdict change. La bascule est **atomique côté base** par un index d'unicité **PARTIEL sur `Issued`** (jamais une neutralisation applicative du SIREN) qui PRÉSERVE le remplacement F06 §4.3 (cohérent avec INV-DOCUMENTS-006). Colonne `mandant_id` ADDITIVE nullable, sans FK vers `mandats` (frontière par les Contracts, CLAUDE.md n°6). | `DuplicateCheckRequest.MandantId` (Contracts) + branche `PostgresDuplicateDocumentCheck` (`PriorsByMandantAndNumberSql` vs `PriorsBySupplierAndNumberSql`) ; `Document.MandantId` (préservé en read-modify-write par `PostgresDocumentUnitOfWork`) ; migration `V010__add_mandant_id_and_unique_index_389.sql` (colonne + index partiel `uq_documents_mandant_number_issued_389`) ; tests `SelfBilled389AntiDuplicateIntegrationTests.*` (cartésien mandants/ré-extraction, parité §4.3, non-régression non-389, unicité atomique + index partiel). La PROJECTION du type 389 + parties vers le payload PA (émission réelle) est portée par MND07. |
| INV-DOCUMENTS-014 | Les ports déclarés par Validation et implémentés ici (TRK03) sont TENANT-SCOPÉS PAR LA CONNEXION (database-per-tenant — `companyId` est honoré par la connexion scopée, aucune colonne de tenant, aucune lecture cross-tenant — CLAUDE.md n°9/17) et FAIL-SAFE : `IIssuedDocumentLookup` ne déclare émis qu'un document réellement en état Issued ; `IIssuedInvoiceLookup` rapproche par numéro **ET date** d'origine (EN 16931 BT-25 + date) — le `document_number` n'étant PAS unique en base (remplacement F06 §4), la date est REQUISE pour discriminer (F07-F08 §B.2.5 « date d'origine obligatoire si la référence n'est pas unique » ; sans elle le rapprochement serait fail-OPEN) — et retourne `Unknown` (avoir orphelin, blocage en aval) quand la référence est absente/inconnue — jamais de référence fabriquée (CLAUDE.md n°2/3). | `IssuedDocumentLookup` (EXISTS … state = Issued) + `IssuedInvoiceLookup` (EXISTS issued / EXISTS known sur `document_number = @Number AND issue_date = @IssueDate` → KnownIssued/KnownNotIssued/Unknown) sur `IConnectionFactory` scopé ; tests `AntiDuplicateIntegrationTests.IssuedDocumentLookup_*`, `IssuedInvoiceLookup_Maps_Original_Invoice_State`, `IssuedInvoiceLookup_Discriminates_By_Issue_Date_When_Number_Collides`. |

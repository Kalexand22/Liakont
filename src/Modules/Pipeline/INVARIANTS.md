# Pipeline Module — Invariants

| ID | Rule | Enforcement |
|---|---|---|
| INV-PIPELINE-001 | Le lecteur canonique est le MIROIR EXACT du writer du contrat : `Serialize(Read(json)) == json` octet par octet, échelle `decimal` préservée, énumérations par nom, dates `yyyy-MM-dd`, optionnels omis, collections toujours présentes | `PivotCanonicalJsonReader` (parse du texte brut des décimaux, `Enum.Parse` par nom) — testé `PivotCanonicalJsonReaderTests` (round-trip sur les 8 golden de document unique) |
| INV-PIPELINE-002 | Le lecteur NE RE-SÉRIALISE PAS pour ré-hacher : la re-vérification du `payload_hash` est faite par `IPayloadStagingStore.ReadAsync` sur la string brute (PIP00) | `PivotCanonicalJsonReader.Read` (pure désérialisation, sans hash) ; doc de classe |
| INV-PIPELINE-003 | Aucune règle fiscale inventée : le triplet {catégorie, taux, VATEX} vient TOUJOURS de la table validée du tenant (moteur `TvaMapper`). CHECK ne devine jamais adjudication/frais : il fournit `TvaMappingPart.Autre` (« hors du découpage » — fidèle à l'absence de cette distinction dans le pivot générique, ADR-0004/F03 §2.3) | Revue (P1, CLAUDE.md n°2) ; `CheckTvaMapping` (doc + `LinePart = Autre`) — testé `CheckTvaMappingTests` |
| INV-PIPELINE-008 | CHECK est SÛR PAR DÉFAUT : un régime/part absent de la table validée, une table absente, une ligne hors forme V1 (≠ 1 code régime ↔ 1 ventilation) ou une anomalie de validation BLOQUANTE ⇒ document `Blocked`, JAMAIS d'envoi à l'aveugle ni d'association régime→ventilation devinée. La décision (mapping → garde-fou → validation) est centralisée dans `DocumentCheckEvaluator`, source UNIQUE partagée par le CHECK et la réconciliation des avoirs (INV-PIPELINE-026) | `DocumentReceivedConsumer` → `DocumentCheckEvaluator` + `CheckTvaMapping.Evaluate` — testé `DocumentReceivedConsumerTests` / `CheckTvaMappingTests` |
| INV-PIPELINE-009 | GARDE-FOU PRODUCTION (PIP01b §3) : si un compte PA ACTIF est en environnement « Production » ET la table TVA n'est PAS validée ⇒ TOUS les documents restent `Blocked`. Jamais affaiblie en Warning (P1) | `DocumentCheckEvaluator.IsProductionContextAsync` + `mapping.IsValidated` — testé `DocumentReceivedConsumerTests` ; CLAUDE.md n°3 |
| INV-PIPELINE-010 | CHECK est IDEMPOTENT (livraison at-least-once) : il n'agit que sur un document encore `Detected` (rejeu d'un document avancé = no-op). Un contenu pas encore stagé (`StagedPayloadNotFoundException`) est TRANSITOIRE (ADR-0014) : propagé pour re-livraison, JAMAIS un blocage terminal | `DocumentReceivedConsumer` (pré-contrôle `Detected` ; `StagedPayloadNotFound` re-levée) — testé `DocumentReceivedConsumerTests` |
| INV-PIPELINE-011 | Les motifs de blocage sont persistés dans la piste d'audit APPEND-ONLY du module Documents via `IDocumentLifecycle.BlockAsync` ; CHECK ne fait aucun update/delete d'audit, et toute transition est tenant-scopée | `DocumentReceivedConsumer` (transitions via `IDocumentLifecycle` uniquement) ; CLAUDE.md n°4/12 |
| INV-PIPELINE-012 | Une exécution CHECK consigne UNE trace clôturée (`PipelineRunType.Check`, `PipelineRunTrigger.Event`) dans `pipeline.run_logs` du tenant ; les exécutions transitoires (non stagé / tenant non configuré) n'écrivent pas de trace (exécution non clôturée) | `DocumentReceivedConsumer` (`RunLog.Start/Complete` + `IPipelineRunLogStore.SaveAsync`) — testé `DocumentReceivedConsumerTests` |
| INV-PIPELINE-013 | Un contenu stagé altéré/illisible (`StagedPayloadIntegrityException` — empreinte ≠ attendue OU blob indéchiffrable) est PERSISTANT : CHECK BLOQUE le document avec un motif (visible opérateur), JAMAIS un dead-letter silencieux laissant le document figé en `Detected` (« bloquer plutôt qu'envoyer faux ») | `DocumentReceivedConsumer` (catch `StagedPayloadIntegrityException` → `BlockAndLogAsync`) — testé `DocumentReceivedConsumerTests` ; CLAUDE.md n°3 |
| INV-PIPELINE-004 | Le module n'accède aux autres modules que par leurs `Contracts` (jamais `Domain`/`Application`/`Infrastructure`), et ne référence aucune PA concrète | Références de projet (`Infrastructure.csproj`) ; module-rules §3 ; revue (P1, CLAUDE.md n°14) |
| INV-PIPELINE-005 | Le journal d'exécutions `pipeline.run_logs` est TENANT-SCOPÉ par la connexion (database-per-tenant) ; aucune lecture cross-tenant | `PostgresPipelineRunQueries` (via `IConnectionFactory`) ; CLAUDE.md n°9/17 |
| INV-PIPELINE-006 | Une exécution (`RunLog`) ne peut pas se clôturer avant son début, ni porter un compteur négatif | `RunLog.Complete` (gardes `completedAt >= StartedAt`, compteurs ≥ 0) — testé `RunLogTests` |
| INV-PIPELINE-007 | `run_type` / `run_trigger` sont persistés par NOM d'énumération et relus tels quels (fidélité base ↔ contrat) | `PostgresPipelineRunQueries.MapRun` (`Enum.Parse`) — testé `PipelineRunLogQueriesIntegrationTests` |
| INV-PIPELINE-014 | SEND est un job PAR TENANT (`ITenantJob` exécuté par `ITenantJobRunner`, SOL06) ; AUCUNE boucle multi-tenant locale, et « un seul job send/sync par tenant à la fois » est garanti par l'ordonnanceur (pas de mutex applicatif) | `SendAllFanOutHandler` (`RunForAllTenantsAsync`) + `SendTenantJob` — testé `SendAllFanOutHandlerTests` ; CLAUDE.md n°9 |
| INV-PIPELINE-015 | DIAGNOSTIC PA (PIP01c §3, F04 §3.1, « Transport not available ») : SIREN non publié / `tax_report_setting` inactif (`StartDate` absente ou future) ⇒ AUCUN envoi (documents maintenus `ReadyToSend`) + Warning. Jamais d'envoi à l'aveugle. Le diagnostic est PILOTÉ PAR CAPACITÉ (jamais `if (pa is …)`) : une PA de niveau « Essentiel » (`SupportsFacturXTransmission`, FX07) n'a AUCUN `tax_report_setting` (email/dépôt) et n'est PAS soumise à ce gate — sinon son réglage neutre faute « Transport not available » et elle ne transmettrait jamais | `SendTenantJob.ExecuteAsync` (gate `!SupportsFacturXTransmission` autour de `IsTaxReportSettingActive`) — testé `SendTenantJobTests` / `SendTenantJobIntegrationTests` ; CLAUDE.md n°3/8 |
| INV-PIPELINE-016 | ANTI-DOUBLON inter-traitement (TRK03, F05) : avant tout renvoi d'un `Sending` (crash) ou retry d'un `TechnicalError`, si le document porte une référence PA et que la PA le déclare `Issued` ⇒ `MarkIssued` SANS renvoyer ; sinon le renvoi est dédoublonné par la PA (numéro de document) — JAMAIS une double émission | `SendTenantJob.TryFinalizeFromPaStatusAsync` + idempotence PA (F05) — testé `SendTenantJobTests` / `SendTenantJobIntegrationTests` |
| INV-PIPELINE-017 | ÉMISSION : sur `Issued` ⇒ `MarkIssued` (preuve) + archive WORM (TRK05) ; la PURGE du staging est SUBORDONNÉE à la présence EFFECTIVE du paquet WORM (`IStagingPurgeService` → `IArchivedDocumentProbe` → `IArchiveStore.ExistsAsync`), JAMAIS à la seule étiquette `Issued` (entre la transition et l'écriture WORM = staging CONSERVÉ) | `SendTenantJob.FinalizeIssuedAsync` (`PurgeIfArchivedAsync`) — testé `SendTenantJobIntegrationTests` (Issued + WORM absent = conservé) ; ADR-0014 §4 |
| INV-PIPELINE-018 | REJET PA (`RejectedByPa`) et ERREUR TECHNIQUE (`TechnicalError`) CONSERVENT le staging (contenu requis pour correction/resoumission, jamais archivé en WORM) ; aucune purge sur rejet/erreur | `SendTenantJob.HandleSendResultAsync` — testé `SendTenantJobTests` / `SendTenantJobIntegrationTests` ; ADR-0014 |
| INV-PIPELINE-019 | La preuve d'échange PA (`pa_response_snapshot`, colonne `jsonb`) est TOUJOURS du JSON valide : la réponse brute de la PA — potentiellement non-JSON — est EMBARQUÉE dans un champ chaîne `rawResponse`, jamais réinterprétée | `SendPaSnapshot` (`JsonSerializer`) — testé `SendTenantJobIntegrationTests` (rejet PA en texte brut archivé) |
| INV-PIPELINE-020 | DRY-RUN (PIP01c §5) : « tout sauf écritures PA » — dénombre les `ReadyToSend`, n'appelle AUCUNE écriture PA (`SendDocumentAsync`/`Ensure…`) et ne fait avancer aucun document ; un `RunLog` SEND est tout de même consigné | `SendTenantJob.ExecuteAsync` (branche dry-run) — testé `SendTenantJobTests` |
| INV-PIPELINE-021 | Un avoir (`CreditNoteRefs` non vide) vers une PA sans capacité avoirs reste `ReadyToSend` (traité par le pipeline des avoirs, PIP02), JAMAIS bloqué ni envoyé à l'aveugle (l'état machine interdit `Sending → ReadyToSend`) | `SendTenantJob.SendReadyAsync` (garde capacité avant `BeginSending`) ; piloté par `PaCapabilities.SupportsCreditNotes` |
| INV-PIPELINE-022 | SYNC (PIP01d) est un job PAR TENANT (`ITenantJob`/`ITenantJobRunner`, SOL06) de RÉCONCILIATION en lecture seule : pour chaque `Issued`, récupère la facture PA générée (SI `SupportsDocumentRetrieval`) et le(s) tax report(s) (SI `SupportsTaxReportRetrieval`) → ADDENDA WORM (TRK05). Piloté par les CAPACITÉS déclarées, jamais par `if (pa is …)` ni un flag produit | `SyncTenantJob` + `SyncAllFanOutHandler` — testé `SyncTenantJobIntegrationTests` (avec/sans capacités) ; CLAUDE.md n°8/16 |
| INV-PIPELINE-023 | ATTRIBUTION TAX REPORT — PAR DOCUMENT, jamais inventée : un tax report n'est rattaché à un document que s'il figure dans les `tax_report_ids` que la PA renvoie POUR CE document (`GetDocumentStatusAsync`) ; le contenu vient de `ListTaxReportsAsync`. Les addenda sont IDEMPOTENTS (adressage par empreinte de contenu) — un SYNC ré-exécuté ne duplique jamais | `SyncTenantJob.SyncDocumentAsync` + `IArchiveService.AddAddendumAsync` (chemin dérivé du hash) — testé `SyncTenantJobIntegrationTests` (idempotence) ; CLAUDE.md n°2/4 |
| INV-PIPELINE-024 | SYNC ne fait avancer AUCUNE machine à états, n'écrit RIEN dans la base source et n'effectue AUCUNE transmission ; un tax report dont le XML n'est pas encore disponible est repris au cycle suivant (jamais un échec), un XML base64 illisible est ignoré (jamais un addendum corrompu archivé) | `SyncTenantJob` (addenda seuls ; `Convert.FromBase64String` gardé) ; coffre WORM append-only inchangé |
| INV-PIPELINE-025 | POINT DE STATUT AGENT (ADR-0012, `GET /documents/status`) : `Processed` SSI un Document existe pour la clé `(source_reference, payload_hash)` (Detected ET au-delà, Issued INCLUS — un Issued, staging purgé, répond TOUJOURS Processed) ; sinon `Pending`. Une clé inconnue sur la route existante = 200 + `Pending`, JAMAIS 404 ; lecture seule, tenant-scopé. La valeur `Rejected` n'est jamais retournée (rejet d'intake = 400 synchrone, rien persisté) | `GetDocumentIntakeStatusHandler` (null→Pending, présent→Processed) + endpoint Host (toujours 200) — testé `GetDocumentIntakeStatusHandlerTests` |
| INV-PIPELINE-026 | RÉORDONNANCEMENT DES AVOIRS (PIP02, F07-F08 §B.5) : un avoir resté `Blocked` parce que sa facture d'origine n'était pas (encore) émise est RÉ-ÉVALUÉ par le SEND APRÈS l'émission des originaux ; s'il passe désormais il est débloqué (`Blocked → ReadyToSend`) et émis DANS LE MÊME TRAITEMENT — TOUJOURS après sa facture d'origine. Re-évaluation COMPLÈTE (`DocumentCheckEvaluator` : mapping + garde-fou + validation), jamais un simple test d'origine (un avoir bloqué pour un AUTRE motif RESTE bloqué — « bloquer plutôt qu'envoyer faux ») ; scope STRICT aux avoirs (`CreditNoteRefs` non vide) ; un avoir orphelin (origine inconnue) reste `Blocked`, jamais d'envoi ni de référence fabriquée. La seule transition est `Blocked → ReadyToSend` (jamais `Blocked → Blocked`) : pas de churn de la piste d'audit append-only | `SendTenantJob.ReconcileCreditNotesAsync` (2e passe d'envoi conditionnelle) + `DocumentCheckEvaluator` — testé `CreditNotePipelineTests` ; CLAUDE.md n°2/3/4 |
| INV-PIPELINE-027 | AVOIR sans capacité PA (rappel INV-PIPELINE-021) : un avoir (`CreditNoteRefs` non vide) vers une PA sans `SupportsCreditNotes` reste `ReadyToSend` (jamais bloqué ni envoyé) ; il N'EST PAS re-bloqué par la réconciliation (scope avoirs ≠ re-blocage) — il part dès que la capacité est déclarée. Le cas de l'avoir dont SEULE la source porte le type « avoir » SANS aucune référence d'origine (`SourceDocumentKind`) N'EST PAS couvert : la correspondance type-source → avoir n'est PAS spécifiée (varie par logiciel) — ne pas l'inventer (CLAUDE.md n°2 ; trou connu tracé F07-F08 §B.0) | `SendTenantJob.SendReadyAsync` (garde capacité) + revue ; piloté par `PaCapabilities.SupportsCreditNotes` — testé `SendTenantJobIntegrationTests` |

## E-reporting de paiement — snapshot de ventilation (ADR-0015, PIP03a)

| ID | Rule | Enforcement |
|---|---|---|
| INV-VENTILATION-001 | Le snapshot ne contient QUE la ventilation issue du mapping VALIDÉ (taux source/table, base HT, TVA par ligne) ; aucune valeur n'est dérivée ni devinée (P1 si violé — règle fiscale inventée). Le taux non résolu reste `null` (jamais calculé) | `CheckTvaMapping.BuildVentilation` (taux = source `?? mapping`, montants reportés sans recalcul) — testé `VentilationLineTests`, `DocumentReceivedConsumerTests` ; CLAUDE.md n°2 |
| INV-VENTILATION-002 | Montants en `decimal`, jamais float/double : le snapshot sérialise base/TVA/taux en CHAÎNES invariantes dans le jsonb `lines` (jamais de nombre flottant) et les reparse en `decimal` — PRÉCISION INTÉGRALE préservée, aucune troncature ni arrondi silencieux à la capture (l'arrondi commercial half-up n'intervient qu'au CALCUL d'agrégation, INV-PIPELINE-030) | `VentilationLine` (`decimal`) + `PostgresVentilationSnapshotStore` (decimals en string, round-trip) — testé `VentilationLineTests` ; CLAUDE.md n°1 |
| INV-VENTILATION-003 | `pipeline.ventilation_snapshots` est APPEND-ONLY, versionné par `mapping_version` : aucun chemin d'update/delete, triggers base rejetant UPDATE/DELETE/TRUNCATE ; écriture IDEMPOTENTE sur (document_id, mapping_version) (`ON CONFLICT DO NOTHING`) — un re-CHECK n'insère pas de doublon, un re-mapping AJOUTE une version | Migration `V003` (triggers) + `PostgresVentilationSnapshotStore.SaveAsync` — CLAUDE.md n°4 |
| INV-VENTILATION-004 | Le snapshot est TENANT-SCOPÉ par la connexion (database-per-tenant) ; un tenant ne lit jamais le snapshot d'un autre | `PostgresVentilationSnapshotStore` (via `IConnectionFactory`) ; schéma sans colonne tenant ; CLAUDE.md n°9/17 |
| INV-VENTILATION-005 | Le snapshot ne CLASSE PAS frais/adjudication d'un document `Mixte` (réservé à PIP03b, gelé sur D-b) ; il capture la ventilation PAR TAUX telle que produite, sans la compléter — un `Mixte` reste SUSPENDU pour l'e-reporting de paiement | `PaymentAggregationCalculator` (Mixte → `MixteSuspended`) — testé `PaymentAggregationCalculatorTests` ; ADR-0015 §2 |
| INV-VENTILATION-006 | Le snapshot est une projection DURABLE distincte du staging (purgé après Issued, ADR-0014) et du coffre WORM (immuable) ; il SURVIT à la purge du staging — l'agrégation post-émission reste requêtable | Persistance dédiée `pipeline.ventilation_snapshots` ; testé `PaymentAggregationIntegrationTests` (snapshot lu après purge du staging) |

## E-reporting de paiement — agrégation jour×taux (PIP03a, F09 §2)

| ID | Rule | Enforcement |
|---|---|---|
| INV-PIPELINE-028 | L'agrégation est un job PAR TENANT (`ITenantJob`/`ITenantJobRunner`, SOL06) ; AUCUNE boucle multi-tenant locale. C'est une PROJECTION recalculée (lecture seule des encaissements + snapshots), aucune transmission, aucune machine à états avancée — le fenêtrage en période et l'envoi réel sont PIP03b | `PaymentAggregatorTenantJob` + `AggregatePaymentsAllFanOutHandler` — testé `PaymentAggregationIntegrationTests` ; CLAUDE.md n°9 |
| INV-PIPELINE-029 | Seuls les documents MONO-CATÉGORIE `PrestationServices` sont agrégés (ventilation par taux directement utilisable) ; un `Mixte` est SUSPENDU (D-b non sourcé), une `LivraisonBiens` est NON CONCERNÉE (exigibilité à la livraison), un taux non résolu / total nul / règlement non rattaché est ÉCARTÉ avec motif opérateur — jamais une part devinée | `PaymentAggregationCalculator` (exclusions typées) — testé `PaymentAggregationCalculatorTests` ; CLAUDE.md n°2 |
| INV-PIPELINE-030 | La ventilation d'un encaissement reprend la PROPORTION par taux du document (F09 §2 : « montant encaissé ventilé par taux »), jamais un lettrage ligne à ligne inventé ; arrondi commercial half-up à 2 décimales (`PivotRounding.RoundAmount`) ; les montants peuvent être négatifs (remboursement, F09 §5.4) | `PaymentAggregationCalculator.Aggregate` (couverture = montant/total, par taux) — testé `PaymentAggregationCalculatorTests` (partiel, remboursement, arrondi) ; CLAUDE.md n°1 |
| INV-PIPELINE-031 | La QUALIFICATION fiscale (Calculated / Suspended / NotRequired / PendingCapability) est portée par la projection Pipeline (`pipeline.payment_aggregations`), JAMAIS par la machine à états opérationnelle du module Payments (INV-PAYMENTS-007). `FeeImputationMethod` ou un paramètre fiscal `null` ⇒ Suspended (jamais de prorata par défaut) ; `vatOnDebits=true` ⇒ NotRequired ; capacité PA absente ⇒ PendingCapability — dans TOUS les cas les agrégats sont CALCULÉS pour la traçabilité | `PaymentAggregationCalculator.QualifyTenant` + `PostgresPaymentAggregationStore` — testé `PaymentAggregationCalculatorTests`, `PaymentAggregationIntegrationTests` ; CLAUDE.md n°2/3 |
| INV-PIPELINE-032 | `pipeline.payment_aggregations` est une PROJECTION recalculée INTÉGRALEMENT à chaque exécution (purge + ré-insertion ATOMIQUE), NI table d'audit NI WORM : aucune clé (jour, taux) périmée ne survit si un encaissement est re-daté/retiré côté source. La piste d'audit immuable des transmissions reste `payments.payment_aggregate_events` (écrite par PIP03b à l'envoi). Tenant-scopé par la connexion | Migration `V004` (uq date/taux, pas de trigger append-only) + `PostgresPaymentAggregationStore.ReplaceAllAsync` (DELETE + INSERT en transaction) ; CLAUDE.md n°4/9 |

## E-reporting de paiement — rectificatifs (flux RE annule-et-remplace, PIP04, F07-F08 §B.1)

| ID | Rule | Enforcement |
|---|---|---|
| INV-PIPELINE-033 | RECTIFICATIF RE = ANNULE-ET-REMPLACE (F07-F08 §B.1) : un rectificatif reconstruit l'agrégat COMPLET de la période (toutes les lignes REPORTABLES `Calculated` des bornes, triées jour×taux), JAMAIS un delta ni un montant négatif isolé. Il ne RECALCULE rien — reprise FIDÈLE des agrégats de la projection PIP03a (aucune règle fiscale inventée). Le fenêtrage (bornes ↔ période déclarative) reste PIP03b : ici les bornes sont une ENTRÉE | `RectificationBuilder.Build` — testé `RectificationBuilderTests`, `ReportRectificationIntegrationTests` ; CLAUDE.md n°2 |
| INV-PIPELINE-034 | IDEMPOTENCE par EMPREINTE de contenu (SHA-256 déterministe, insensible à l'échelle décimale) : un rectificatif au contenu identique à la dernière transmission ne re-transmet pas ; un changement d'agrégat (avoir sur période déclarée — PIP02 ; altération source — TRK03) ré-ouvre. Capacité de rectification ABSENTE ⇒ EN ATTENTE (`PendingCapability`), JAMAIS d'envoi à l'aveugle ni de blocage produit — piloté par `SupportsReportRectification`, jamais `if (pa is …)` | `ReportRectificationService` (empreinte + gate capacité) — testé `ReportRectificationIntegrationTests` (idempotence, PA sans capacité) ; CLAUDE.md n°8/16 |
| INV-PIPELINE-035 | `pipeline.report_rectifications` est APPEND-ONLY (triggers base rejetant UPDATE/DELETE/TRUNCATE) : l'historique COMPLET (déclaration initiale + chaque rectificatif) est conservé, l'ancien état n'est JAMAIS effacé. DISTINCT de la projection (recalculée) ET de `payments.payment_aggregate_events` (transmission, PIP03b). Montants/taux du `payload_snapshot` en CHAÎNES invariantes (jsonb), jamais float | Migration `V005` (triggers) + `PostgresReportRectificationLedger` — testé `ReportRectificationIntegrationTests` (append-only, historique) ; CLAUDE.md n°1/4 |
| INV-PIPELINE-036 | Une période sans donnée reportable ne donne PAS lieu à une déclaration « néant » par défaut (F09 §5.4, décision OUVERTE) : aucune transmission inventée, signalement opérateur (`NothingToDeclare`). Une période DÉJÀ DÉCLARÉE devenue vide (encaissements re-datés/retirés à la source) est ALERTÉE (log Warning + compteur de run dédié « vidée à statuer »), JAMAIS bucketisée en « inchangée ». La rectification est un job PAR TENANT (`ITenantJob`/`ITenantJobRunner`, SOL06 ; AUCUNE boucle multi-tenant maison) ré-évaluant les périodes DÉJÀ DÉCLARÉES (projection chargée UNE fois par run) ; tenant-scopé ; le pipeline ne référence aucun plug-in PA concret | `ReportRectificationService` (rebuild vide ⇒ pas d'envoi, alerte si déjà déclarée) + `ReportRectificationTenantJob` + `RectifyReportsAllFanOutHandler` — testé `ReportRectificationIntegrationTests` (job) ; CLAUDE.md n°2/9 |
| INV-PIPELINE-037 | FRONTIÈRE FORWARD-COMPAT PIP03b : `SendPaymentReportAsync` ne porte aujourd'hui que `{Flux, bornes}`. L'ENRICHISSEMENT des lignes rectifiées (F09 §5.3) ET un MARQUEUR de flux RE (annule-et-remplace, distinct d'une déclaration initiale) sont la responsabilité de PIP03b ; le statut `Transmitted` du journal = MÉCANISME exécuté, le `payload_snapshot` est la PHOTO du contenu qui sera transmis. PIP03b doit propager le marqueur RE + les lignes (ne pas transmettre une initiale à la place d'un rectificatif) | `ReportRectificationService` (TODO(PIP03b) au point d'envoi + `payload_snapshot` audit) ; périmètre re-découpage PIP03 (v12) |

## Génération Factur-X à l'étape Sending (FX07, F16 §6.1/§7)

| ID | Rule | Enforcement |
|---|---|---|
| INV-PIPELINE-038 | GÉNÉRATION pilotée par CAPACITÉ : quand la PA active déclare `SupportsFacturXTransmission` (jamais `if (pa is …)`, CLAUDE.md n°8), le SEND génère le Factur-X scellé À L'ÉTAPE `Sending`, JUSTE AVANT l'appel de transmission, via le pont `IFacturXArtifactBuilder` (résolu au Host, délègue à `IFacturXBuilder` — frontière Contracts-only respectée, module-rules §3). JAMAIS dans `FinalizeIssuedAsync` (en aval du retour Issued : y générer produirait l'artefact APRÈS l'envoi = faux-vert). La génération bloque (lève) si un BT obligatoire manque (ADR-0023 INV-FX-2). S'applique aux 3 chemins d'envoi (ReadyToSend, recover Sending, retry TechnicalError) | `SendTenantJob.TransmitAsync` (gate `SupportsFacturXTransmission`) — testé `SendTenantJobTests` ; CLAUDE.md n°3/8 |
| INV-PIPELINE-039 | L'artefact PRÉ-CONSTRUIT est passé au plug-in via `PaSendContext` (contrat `IPaClient` étendu) ; les PA Pilotage l'IGNORENT (payload propre, chemin inchangé), la PA générique l'EXIGE (absent → blocage côté plug-in, jamais régénéré — indépendance plug-in CLAUDE.md n°6). Le plug-in transmet l'artefact tel quel | `SendTenantJob.TransmitAsync` + `GeneriqueClient.SendDocumentAsync` — testé `SendTenantJobTests` / `GeneriqueClientTests` |
| INV-PIPELINE-040 | Sur TRANSMISSION FACTUR-X RÉUSSIE (Issued) UNIQUEMENT : journalisation de l'envoi (F06 APPEND-ONLY via `IPaTransmissionJournal` — compte/plug-in PA, horodatages, empreinte SHA-256 de l'artefact, clé d'idempotence = numéro de document, réponse PA) PUIS trace de support (`ISupportTraceStore` — copie du Factur-X transmis, store dédié purgeable, tenant-scopé). Aucun chemin update/delete sur `document_events` (CLAUDE.md n°4). Le chemin Pilotage (pas d'artefact) NE journalise NI ne trace (comportement inchangé) ; un rejet/erreur ne journalise pas (aucun artefact « transmis ») | `SendTenantJob.FinalizeIssuedAsync` (branche `facturX is not null`) — testé `SendTenantJobTests` ; journal réel testé `PaTransmissionJournalServiceIntegrationTests` ; CLAUDE.md n°4/9 |

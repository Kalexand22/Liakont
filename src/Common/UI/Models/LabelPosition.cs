namespace Stratum.Common.UI.Models;

/// <summary>
/// Position of a label relative to its associated control.
/// </summary>
public enum LabelPosition
{
    /// <summary>Label before the control (left in LTR).</summary>
    Start,

    /// <summary>Label after the control (right in LTR).</summary>
    End,
}

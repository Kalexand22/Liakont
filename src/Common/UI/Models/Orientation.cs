namespace Stratum.Common.UI.Models;

/// <summary>
/// Layout orientation for grouped controls (radio groups, checkbox groups).
/// </summary>
public enum Orientation
{
    /// <summary>Stack items vertically (default).</summary>
    Vertical,

    /// <summary>Lay out items horizontally in a row.</summary>
    Horizontal,
}

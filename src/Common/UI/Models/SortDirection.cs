namespace Stratum.Common.UI.Models;

/// <summary>Sort direction for SimpleTable columns.</summary>
public enum SortDirection
{
    /// <summary>Ascending order (A → Z, 0 → 9).</summary>
    Ascending,

    /// <summary>Descending order (Z → A, 9 → 0).</summary>
    Descending,
}

namespace Stratum.Common.UI.Models;

/// <summary>Horizontal text alignment for grid columns.</summary>
public enum TextAlign
{
    /// <summary>Left-aligned (default).</summary>
    Left,

    /// <summary>Center-aligned.</summary>
    Center,

    /// <summary>Right-aligned (typical for numeric values).</summary>
    Right,
}

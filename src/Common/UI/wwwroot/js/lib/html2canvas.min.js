/*! html2canvas v1.4.1 — STUB for development.
 *  Replace with the official minified build from https://html2canvas.hertzen.com/
 *  npm install html2canvas  →  copy node_modules/html2canvas/dist/html2canvas.min.js here.
 */
(function (global, factory) {
    "use strict";
    if (typeof define === "function" && define.amd) {
        define([], factory);
    } else if (typeof module !== "undefined" && module.exports) {
        module.exports = factory();
    } else {
        global.html2canvas = factory();
    }
})(this, function () {
    "use strict";

    /**
     * Stub implementation: captures the target element as a plain white canvas
     * with the correct dimensions. Sufficient for compile-time and structural
     * verification; replace with real html2canvas for runtime screenshot fidelity.
     */
    return function (element, options) {
        options = options || {};
        return new Promise(function (resolve) {
            var canvas = document.createElement("canvas");
            canvas.width  = options.width  || (element ? element.offsetWidth  : document.body.offsetWidth)  || 1;
            canvas.height = options.height || (element ? element.offsetHeight : document.body.offsetHeight) || 1;
            var ctx = canvas.getContext("2d");
            ctx.fillStyle = "#ffffff";
            ctx.fillRect(0, 0, canvas.width, canvas.height);
            resolve(canvas);
        });
    };
});

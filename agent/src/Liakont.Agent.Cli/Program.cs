namespace Liakont.Agent.Cli;

using System;

/// <summary>
/// CLI de diagnostic de l'agent : check-config, test ODBC, test API, run manuel
/// (blueprint.md §4). Placeholder : les commandes réelles arrivent avec les items AGT.
/// </summary>
internal static class Program
{
    private static void Main()
    {
        Console.WriteLine("Liakont Agent CLI — diagnostic (placeholder, voir items AGT).");
    }
}
